# 接口自动化 19 节课学习总结

> 依据当前仓库的 19 个课程文件夹，结合每节课的 `课堂笔记.md`、Python 代码、YAML/Excel 用例整理。

按当前 19 个文件夹，一节课一节整理如下。整体学习主线是：

`Requests 基础 → 接口关联与封装 → Pytest/YAML 框架 → 全局变量与前后置脚本 → 多用例与提取 → 断言 → 数据库 → 数据驱动 → 日志/报告 → Session → 上传下载加密 → Excel → 插件 → Mock`

## 一、Requests 基础与接口流程

| 课次 | 课程主题 | 这节课讲了什么 | 主要代码/文件 |
|---|---|---|---|
| 01 | 8月24日 3.3 Requests接口自动化库应用 | API 测试框架的作用；安装 `requests`；PyCharm 虚拟环境配置；`requests.request/get/post`；GET 与 POST 区别；`params`、`data`、`json`、`headers` 的传参位置；上传下载接口和 Flask 初识 | `requests请求.py` |
| 02 | 8月25日 3.4 Requests接口自动化流程实战 | requests 每次请求相互独立；cookie、session、token 的区别与适用场景；`requests.session()` 保持会话；token 鉴权要手动放请求头；JSONPath 提取数据，重点掌握 `$`、`.`、`..`、`[index]`，以及结果始终是列表 | `jsonpath的应用.py` |
| 03 | 8月26日 3.5 接口自动化接口流程实战 | 用“登录 → 加购物车 → 查看购物车 → 查看地址 → 提交订单”串起接口关联；为什么要二次封装 requests；`Keywords` 类；`**kwargs`、`kwargs.get`、data/json 自动切换、`try/except`；传 requests 或 session 的鸭子类型 | `接口关联.py`、`二次封装-工作版/api_client.py`、`二次封装-工作版/test_api_flow.py` |

## 二、Pytest/YAML 框架搭建

| 课次 | 课程主题 | 这节课讲了什么 | 主要代码/文件 |
|---|---|---|---|
| 04 | 8月27日 3.6 Pytest+Yaml+Allure框架集成 | `pip freeze` 导出依赖；YAML 基本语法；Windows 路径转义和相对路径；一条用例用字典、一组用例用列表；`pytest.mark.parametrize`；搭出第一版 HAT 框架目录、YAML 读取器、Keywords 和执行器 | `HAT/core/testrun.py`、`HAT/parse/YamlCaseParser.py`、`HAT/keywords/api_client.py`、`examples/api-cases-yaml/1_login.yaml` |
| 05 | 8月28日 3.9 核心执行器优化 | 安装 Allure（`allure-pytest`、`allure-commandline`）；pytest 的 `-v/-s/--capture/--alluredir`；`allure generate/open/serve`；`allure_combine` 合并报告；用基础配置设置标题、一级模块、二级模块；`@allure.step`；tqdm 进度条；从 `HAT/key_dir` 动态加载关键字 | `main.py`、`HAT/core/testrun.py`、`HAT/key_dir/发送请求POST.py`、`HAT/keywords/api_client.py` |
| 06 | 8月29日 3.10 测试环境动态切换机制 | 用 `g_context` 做全局变量；类变量 `_dic` 实现共享；`set_dict`、`set_by_dict`、`get_dict`、`show_dict`；从 `context.yaml` 加载环境地址、账号等；YAML 中用 `{{变量名}}`；Jinja2 `Template` + `refresh` 渲染；执行前 `deepcopy` + `eval` 还原数据 | `HAT/core/globalContext.py`、`HAT/parse/YamlCaseParser.py`、`HAT/utils/VarRender.py`、`HAT/core/TestRunner.py`、`context.yaml` |
| 07 | 8月30日 3.11 前后置条件设计 | 前置脚本准备数据、后置脚本清理数据；脚本里的 `context` 就是全局变量字典；`context.update()` 往全局变量里加值；YAML 中 `前置脚本`、`后置脚本` 可以写空字符串或列表；执行顺序是“全局变量 → 前置脚本 → 渲染步骤 → 接口请求 → 后置脚本”；用 `run_script.exec_script` 动态执行脚本 | `HAT/extend/script/run_script.py`、`HAT/core/TestRunner.py`、`login.yaml` |
| 08 | 8月31日 3.12 YAML多用例管理机制 | 从只读单个 YAML 升级为读取文件夹下多个 YAML；规定“数字开头_分割.yaml”才算用例；`load_yaml_files` 批量加载；接口关联三步：先保存响应到全局变量、再用 JSONPath 提取值、下个接口从全局变量取值；封装“提取数据JSON”关键字 | `HAT/parse/YamlCaseParser.py`、`HAT/keywords/api_keywords.py`、`1_login.yaml`、`2_addcard.yaml` |

## 三、断言、数据库与数据驱动

| 课次 | 课程主题 | 这节课讲了什么 | 主要代码/文件 |
|---|---|---|---|
| 09 | 9月1日 3.13 断言机制设计与实践 | 为什么必须断言：否则执行完 pytest 仍显示成功；`assert 预期 == 实际, "错误信息"`；`==`、`!=`、`>`、`<`、`>=`、`<=`；区分预期结果和实际结果；把断言封装成关键字：断言文本、文本相等/不相等/包含、数字相等/大于/大于等于/小于/小于等于/不等于 | `HAT/keywords/api_keywords.py` 中的断言方法、`1_login.yaml`、`2_addcard.yaml`、`stu.yaml` |
| 10 | 9月2日 3.16 接口自动化批量及数据库断言 | 普通字段断言的不足；用 `deepdiff` 做整个结构深度比对；`values_changed`、缺失字段等差异类型；`exclude_paths` 过滤动态字段；`ignore_string_case` 忽略大小写；`ignore_order` 忽略顺序；`assert not diff`；封装“批量断言”关键字。数据库实操在下一节展开 | `examples_test/deepdiff1_test.py`、`deepdiff2_test.py`、`api_keywords.py` 中的批量断言 |
| 11 | 9月3日 3.17 接口自动化数据库断言 | 为什么接口自动化要查数据库：校验接口结果、准备数据、检查增删改；PyMySQL 操作七步：连接、游标、写 SQL、执行、取结果、关游标、关连接；`DictCursor` 让结果按字典显示；数据库配置放 `context.yaml`，SQL 不写死；封装“提取数据库MYSQL”关键字；多条结果用 `变量名_1`、`变量名_2` 区分 | `examples_test/mysql_test.py`、`HAT/keywords/api_keywords.py` 中的“提取数据库MYSQL”、`context.yaml` |
| 12 | 9月4日 3.18 YAML实现数据驱动测试DDT | 一条用例要测多组数据时，不重复写 YAML；用“用例模板 + 数据驱动”组合；`{{uname}}`、`{{pwd}}`、`{{expected}}` 做占位；YAML 中增加 `数据驱动` 列表；有几组数据就生成几条用例；用例标题 = 原用例标题 + 描述标题；数据通过 `local_context` 传给执行器 | `examples/api-cases-yaml/0_loginddt.yaml`、`YamlCaseParser.yaml_case_parser`、`TestRunner` 中的 `local_context` 渲染 |

## 四、日志、Session 与常用案例

| 课次 | 课程主题 | 这节课讲了什么 | 主要代码/文件 |
|---|---|---|---|
| 13 | 9月5日 3.19 基于Allure与Loguru的日志与报告集成 | 日志的概念、组成和级别；为什么不用 `print`；loguru 的 `debug/info/warning/error`；日志写入文件；在可能出错处加 `try/except` 并记录日志；把步骤日志挂到 Allure 报告；`StepLogCollector` 收集日志；`allure_step_with_log` 同时包住 Allure 步骤和日志 | `examples_test/test_log.py`、`HAT/utils/allure_step_logger.py`、`main.py` 的 `logger.configure`、`api_keywords.show_log` |
| 14 | 9月6日 3.20 Session复用在框架中的应用 | requests 每次请求独立的问题；cookie/session 鉴权和 token 鉴权的区别；`context.yaml` 增加 `session_reuse` 开关；`True` 时只创建一个 `requests.session()` 并复用，`False` 时每次新建；用 `ApiCaseContext.init_keywords()` 统一初始化；继续优化 `key_dir` 关键字外部调用 | `HAT/context/ApiCaseContext.py`、`api_keywords.ex_invoke`、`HAT/core/TestRunner.py` |
| 15 | 9月7日 3.23 Yaml加密、上传、下载案例 | 上传接口四要素：POST、地址、文件、响应；用 `files` 传文件；下载接口用 GET 并把内容保存到本地；加解密概念：对称加密、非对称加密、哈希；后端提供 jar 加密服务时先启动服务再请求；AES 加密后再发登录请求；本地 Flask 上传下载示例 | `upload.yaml`、`download.yaml`、`1_aeslogin.yaml`、`examples_test/加密数据.py`、`md5.py`、`P01_4 flask应用`、`api_keywords` 中的“下载接口get”和“加密aes” |

## 五、Excel、插件与 Mock

| 课次 | 课程主题 | 这节课讲了什么 | 主要代码/文件 |
|---|---|---|---|
| 16 | 9月8日 3.24 基于Excel的接口自动化框架 | 为什么工作中也用 Excel：测试人员更容易维护；Excel 固定列、操作类型对应 keywords、一条用例可以有多个步骤；核心思路是把 Excel 读成和 YAML 一样的结构；用 pandas/openpyxl 读取；`group_cases_by_title` 按“用例标题”合并步骤；正则解析“数据内容”里的 `key=value`；`safe_convert_value` 安全转数字、字典、布尔值；按“数字开头_*.xlsx”规则加载 | `HAT/parse/ExcelCaseParser.py`、`examples/api-cases-excel/1_shop.xlsx`、`HAT/parse/excel数据.yaml` |
| 17 | 9月9日 3.25 Excel动态解析与环境自适应 | 动态数据不写死；用 `context.xlsx` 代替 `context.yaml`；`数据库配置` sheet 转成 `_数据库` 字典；`通用配置` sheet 转成全局变量；`load_context_from_excel` 读取环境、数据库、URL、账号等；Excel 用例解析器统一返回 `case_infos` 和 `case_names`；导出 `context_excel数据.yaml` 验证转换结果 | `HAT/parse/ExcelCaseParser.py`、`examples/api-cases-excel/context.xlsx`、`2_API测试用例.xlsx`、`HAT/parse/context_excel数据.yaml` |
| 18 | 9月10日 3.26 接口自动化自定义插件开发与应用 | 解决每次都要改 `TestRunner.py` 的用例路径和参数问题；Pytest 插件概念；三个核心 Hook：`pytest_addoption` 增加 `--type/--cases/--keyDir`，`pytest_generate_tests` 动态参数化，`pytest_collection_modifyitems` 修复中文显示；`CaseParser` 根据类型分发 YAML/Excel；`main.py` 通过插件运行；框架打包：`setup.py`、`README`、`pip freeze`、`python setup.py install`、`pip install -e .`；命令行运行 `huace-test --type=yaml --cases=...` | `HAT/core/CasesPlugin.py`、`HAT/parse/CaseParser.py`、`main.py`、`HAT/core/TestRunner.py`、`examples/api-cases-商城` |
| 19 | 9月11日 3.27 基于Flask搭建MockServer | Mock 的意义和场景；开发 Mock 前要知道接口地址、请求方式、请求参数、正常/异常返回；Flask 与 Django 对比；Flask 核心结构 `Flask(__name__)`、`@app.route`、`app.run`；GET 用 `request.args`，POST 用 `request.data`，JSON 用 `request.json/get_json`；`debug`、`host`、`port`；从返回字符串逐步升级到 `jsonify` 标准 JSON 响应 | `flask01_test.py`、`flask02_test.py`、`flask03_test.py` |

## 容易混淆的地方

- `04` 的课程标题包含 Allure，但真正的 Allure 报告配置、`@allure.step` 和 `allure_combine` 主要是在 `05` 里展开的；`04` 的重点是第一版 Pytest + YAML 执行器。
- `10` 主要讲 DeepDiff 批量断言，真正的 MySQL 数据库操作和数据库断言在 `11`。
- `17` 生成的 `context_excel数据.yaml` 里写的是 `seesion_reuse`，而运行代码读取的是 `session_reuse`，Excel 的 session 复用配置需要留意这个拼写问题。
- `18` 的 `main.py` 导入的是 `CasePlugin`，但 `CasesPlugin.py` 里定义的类名是 `CasesPlugin`，运行插件时名称需要保持一致。
