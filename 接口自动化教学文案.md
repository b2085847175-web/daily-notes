# 接口自动化教学文案（8月24号 - 9月11号）

> 写给只会一点 Python 基础的同学。  
> 这份文案按“每天课程任务”分成一节一节来讲，重点讲：代码在写什么、为什么要这样写、它解决了什么测试问题、适合在什么场景用。  
> 所有代码都以 `F:\课堂派\接口自动化` 里的课堂资料为准。文案里出现的代码片段只做讲解，不代替源文件。账号、密码、数据库连接信息、token 等敏感值不在这里重复粘贴；写代码时请按老师资料原文填写，不要自己改。

---

## 阅读方式

1. 先看每节开头的“今天解决什么问题”。
2. 再看代码和解释，尽量把代码想象成“流程图”。
3. 看不懂某个 Python 写法时，去本文件最后的《Python 基础卡片》找解释。
4. 课后任务只给题目和提示，不给答案，目的是让你自己动手想。

---

## 全课程主线

这套课程不是零散学几个函数，而是在搭一个接口自动化测试框架：

```text
会发请求
  -> 会提取接口返回值
  -> 会处理接口关联
  -> 会封装请求代码
  -> 会把测试步骤写成 YAML/Excel 用例
  -> 会用全局变量、前后置脚本、数据驱动
  -> 会断言、深度断言、数据库校验
  -> 会日志、报告、Session 复用
  -> 会上传、下载、加密接口
  -> 会 pytest 插件化和 Mock 接口
```

后面所有复杂内容，都是在围绕这个主线服务。

---

# 第1节：8月24号，认识接口自动化和 requests

## 今天解决什么问题

手工测试接口时，你要打开工具、填地址、填参数、点发送、看结果。  
接口自动化就是把这些动作交给代码来做，让代码帮我们“发请求、取结果、做判断、出报告”。

## 先补基础：接口是什么

可以把接口想成一个“服务窗口”：

```text
你带着请求去窗口 -> 服务员处理后 -> 给你一个响应
```

在 HTTP 接口里，这个流程通常是：

```text
请求方法 + 接口地址 + 请求参数 -> 服务端 -> 响应结果
```

常见请求方法有：

- `GET`：一般用于查询。
- `POST`：一般用于提交、新增、修改、删除等操作。

课堂笔记里的重点是：

- `GET` 参数通常拼在 URL 后面，例如 `?a=1&b=2`。
- `POST` 数据一般放在请求体 body 里。
- `POST` 的 body 又分 `data` 和 `json` 两种常见格式。

## 传参位置

课堂资料里总结了三类参数：

| 参数名 | 放在哪里 | 通俗理解 |
|---|---|---|
| `params` | URL 后面 | 页面地址后面带的查询条件 |
| `data` | 请求体 | 表单提交 |
| `json` | 请求体 | JSON 格式提交 |
| `headers` | 请求头 | 告诉服务端“我是谁”“内容是什么类型” |

## requests 的三种写法

课堂资料里提到了三个入口：

```python
requests.request(method, url, **kwargs)
requests.get(url)
requests.post(url)
```

通俗解释：

- `requests.get(url)`：专门发 GET 请求。
- `requests.post(url)`：专门发 POST 请求。
- `requests.request(method, url, **kwargs)`：更通用，`method` 可以是 `"get"`、`"post"` 等。

后面框架为什么喜欢 `self.request.request("post", **request_data)` 这种写法？  
因为它不关心你传进来的是 `requests` 模块还是 `requests.session()` 对象，只要它有 `.request()` 方法，就可以统一调用。

## 安装和准备

```bash
pip install requests
pip list
```

课堂笔记还强调了 PyCharm 的准备：

- 使用虚拟环境 `venv`。
- 选择正确的 Python 解释器。
- 测试运行器选 unittest，这是 PyCharm 自身设置的原因。

## 适合什么场景

- 查询接口、列表接口：先练 GET。
- 登录、下单、保存数据：练 POST。
- 需要判断接口是否返回成功：后面会学断言。

## 课后自测

1. `params` 和 `data` 的区别是什么？
2. 什么时候用 `json=`，什么时候用 `data=`？
3. 一个接口地址很长，`?` 后面的内容叫什么？

---

# 第2节：8月25号，会话、鉴权和 jsonpath

## 今天解决什么问题

很多业务接口不是单独存在的。比如：先登录，再加入购物车，再提交订单。  
这就出现两个问题：

1. 第二个接口怎么知道你已经登录了？
2. 登录接口返回了很多数据，怎么把里面的 `token` 取出来？

## 为什么 requests 每次请求是独立的

课堂资料里说：

> requests 请求每一次请求，都是独立的，相当于每次打开一个新的浏览器。

你可以想象成：

```text
第1次请求：新开浏览器A，登录成功
第2次请求：又新开浏览器B，服务端不认识你
```

所以有些接口会提示未登录。

## cookie、session、token

课堂资料里区分得很清楚：

| 名称 | 存在哪里 | 简单理解 |
|---|---|---|
| cookie | 浏览器本地 | 客户端的小票 |
| session | 服务端 | 服务端记着你的登录状态 |
| token | 客户端自己保存 | 服务端签发的身份字符串 |

判断方法也来自课堂资料：

- 登录返回 cookie / session_id：可以用 `requests.session()`。
- 登录返回 `token`：需要手动把 token 放到请求头或 URL 参数里。

`requests.session()` 的写法是：

```python
session = requests.session()
session.post()
```

它会自动关联和传递 cookie，但它不是万能的。  
如果是 token 鉴权，session 不会自动帮你带 token，还是要自己处理 token。

## jsonpath：从 JSON 里取数据

接口返回的数据通常是 JSON。JSON 里会大量使用：

- 字典：`{}`
- 列表：`[]`
- 字典套列表，列表套字典。

jsonpath 就是用来在复杂 JSON 里找数据的工具。

课堂资料里的核心语法：

| 语法 | 含义 |
|---|---|
| `$` | 根节点，代表整个数据 |
| `.` | 取下一级 |
| `..` | 深层查找，跨层级找 |
| `[index]` | 数组下标，从 0 开始 |

课程里特别强调一个坑：

> jsonpath 取到的结果永远是列表，就算只有一个值也是列表，要拿到具体值需要加 `[0]`。

例如：

```python
year = jsonpath.jsonpath(data, '$..year')[0]
```

这里最后的 `[0]` 不是语法装饰，而是在列表里取第一个元素。

## 课堂代码：`8月25号/jsonpath的应用.py`

这个文件模拟了一个订单接口返回的复杂 JSON，里面有订单、用户、地址、商品等。

代码开头的核心结构是：

```python
data = {
    "code": 200,
    "message": "success",
    "data": {
        "total": 3,
        "page": 1,
        "orders": [
            {
                "order_id": "...",
                "user": {
                    "id": 101,
                    "name": "...",
                    "addresses": [
                        {"type": "home", "city": "..."}
                    ]
                },
                "items": [
                    {"goods_id": 1001, "name": "...", "price": 5999, "count": 1}
                ]
            }
        ]
    }
}
```

后面课堂代码写了第1题的示例：

```python
dingdan = jsonpath.jsonpath(data, '$..order_id')[0]
print(dingdan)
```

这句代码的意思是：

1. `$`：从整个 JSON 开始找。
2. `..order_id`：不管 `order_id` 藏在第几层，都找出来。
3. `jsonpath.jsonpath(...)`：返回一个列表。
4. `[0]`：取列表里的第一个值。

后面的题目只留了题目和提示，没有答案，比如：

- 提取第一个订单的用户名。
- 提取所有商品名称。
- 提取所有用户 id。
- 提取状态为“已完成”的订单。
- 提取第三个订单里第一个商品的名称。

这些题目是在练一件事：看懂 JSON 层级。

## 适合什么测试场景

- 登录后提取 `token`。
- 下单后提取 `order_id`。
- 从列表接口里取第一条数据。
- 从深层返回值里取某个字段。

## 课后自测

1. `$..token` 和 `$.data.token` 有什么区别？
2. 为什么 `jsonpath.jsonpath(...)` 后面经常要加 `[0]`？
3. cookie/session 鉴权和 token 鉴权分别怎么处理？

---

# 第3节：8月26号，接口关联和 requests 二次封装

## 今天解决什么问题

课程做了一个完整流程：

```text
登录 -> 加入购物车 -> 查看购物车 -> 查看地址 -> 提交订单
```

这里有接口关联：

- 登录接口返回 `token`。
- 加入购物车、查看购物车、查看地址、提交订单都需要 `token`。
- 查看购物车返回 `goods_id`，提交订单要用。
- 查看地址返回 `address`，提交订单要用。

也就是说，上一个接口的返回值，是下一个接口的入参。

## 原始写法：`8月26号/接口关联.py`

课堂代码先演示了直接用 `requests` 写业务流程。

登录部分：

```python
import jsonpath
import requests

login_url = '...'
login_data = {
    "accounts": "<测试账号>",
    "pwd": "<测试密码>",
    "type": "username"
}

try:
    requ = requests.post(login_url, data=login_data)
    login_token = jsonpath.jsonpath(requ.json(), '$..token')[0]
    assert jsonpath.jsonpath(requ.json(), '$..msg')[0] == '登录成功', '登陆失败'
except Exception as e:
    raise
```

逐句解释：

- `requests.post(login_url, data=login_data)`：发 POST 请求，用表单方式传登录数据。
- `requ.json()`：把接口响应转成 Python 字典。
- `jsonpath.jsonpath(..., '$..token')[0]`：深层查找 token，并取第一个。
- `assert ... == '登录成功', '登陆失败'`：判断接口是否真的登录成功。
- `try / except`：这里捕获异常后马上 `raise`，意思是“异常先经过这里，再继续抛出去”。

加入购物车部分：

```python
shop_url = f'http://...&token={login_token}'
shop_data = {
    "goods_id": "2",
    "stock": "3"
}

try:
    requ = requests.post(shop_url, data=shop_data)
    assert jsonpath.jsonpath(requ.json(), '$..msg')[0] == '加入成功', '加入失败'
except Exception as e:
    raise
```

重点是 `f'{login_token}'` 这种 f-string 写法，把上一步提取出来的 token 拼进下一个接口地址里。

后面的查看购物车、查看地址、提交订单都是同一套思路：

1. 发请求。
2. 提取需要的数据。
3. 断言 `msg`。
4. 把提取到的值传给下一步。

## 为什么需要二次封装

原始写法的问题很明显：

- 每个接口都要写 `requests.post()` 或 `requests.get()`。
- 每个接口都要处理 url、headers、params、data/json。
- 每个接口都要写异常处理。
- 代码重复太多，改一个地方要改很多处。

课堂资料里的结论：

> 封装的好处：一处修改，处处生效；调用更简单。

## 教学版封装：`8月26号/二次封装-工作版/api_client.py`

这个文件定义了一个 `ApiClient` 类。

```python
import requests


class ApiClient:
    """HTTP 请求客户端封装"""

    def __init__(self, session):
        self.session = session
```

解释：

- `class ApiClient`：定义一个类，可以理解成“模板”。
- `__init__`：创建对象时自动执行。
- `self.session = session`：把外面传进来的 `requests` 或 `requests.session()` 保存到对象里。
- 这里参数名叫 `session`，但它不一定真的是 session，也可以传 `requests` 模块。这是课堂故意讲的鸭子类型思想：只要它能调用 `.request()`，就能用。

### `send_post()` 代码

```python
    def send_post(self, **kwargs):
        url = kwargs.get("url", None)
        data = kwargs.get("data", None)
        params = kwargs.get("params", None)
        headers = kwargs.get("headers", None)
        files = kwargs.get("files", [])
        data_type = kwargs.get("data_type", "data").lower()

        request_data = {
            "url": url,
            "params": params,
            "headers": headers,
            "files": files,
        }

        if data_type == "json":
            request_data["json"] = data
        elif data_type == "data":
            request_data["data"] = data
        else:
            raise Exception("请求类型错误，仅支持json/data")

        try:
            response = self.session.request("post", **request_data)
            return response
        except Exception as e:
            print("post发送请求失败")
            raise
```

这段代码是后面整个框架的基础，必须看懂。

1. `**kwargs`：接收任意多个关键字参数。  
   调用时可以写：

   ```python
   client.send_post(url=..., data=..., data_type="json")
   ```

   也可以少传一些参数：

   ```python
   client.send_post(url=...)
   ```

2. `kwargs.get("url", None)`：从 kwargs 里取值，取不到就给 `None`。  
   这样避免某些参数没传时报 KeyError。

3. `data_type.lower()`：把 `"JSON"` 转成 `"json"`，避免大小写不一致。

4. `request_data` 是统一构造的请求参数字典。  
   它不会一开始就放 `json` 或 `data`，而是根据 `data_type` 决定放哪一个。

5. `self.session.request("post", **request_data)`：
   - 第一个 `"post"` 是请求方法。
   - `**request_data` 是字典解包。
   - 等价于把字典变成 `url=..., params=..., headers=...` 这种关键字参数传进去。

6. `try / except`：请求失败时打印一句提示，然后继续 `raise`。  
   继续抛出的目的是让 pytest 能感知到用例失败。

### `send_get()` 代码

```python
    def send_get(self, **kwargs):
        url = kwargs.get("url", None)
        params = kwargs.get("params", None)
        headers = kwargs.get("headers", None)
        files = kwargs.get("files", [])

        request_data = {
            "url": url,
            "params": params,
            "headers": headers,
            "files": files,
        }

        try:
            response = self.session.request("get", **request_data)
            return response
        except Exception as e:
            print("get发送请求失败")
            raise
```

和 POST 的区别：

- GET 没有 `data_type`。
- GET 一般用 `params`，不是 body。
- 最后调用的是 `self.session.request("get", ...)`。

## 使用封装后的业务代码：`8月26号/二次封装-工作版/test_api_flow.py`

登录部分：

```python
import jsonpath
import requests

from api_client import ApiClient

client = ApiClient(requests)

login_url = '...'
login_data = {
    "accounts": "<测试账号>",
    "pwd": "<测试密码>",
    "type": "username"
}

login_response = client.send_post(url=login_url, data=login_data)
login_token = jsonpath.jsonpath(login_response.json(), '$..token')[0]
assert jsonpath.jsonpath(login_response.json(), '$..msg')[0] == '登录成功', '登录失败'
```

这里把 `requests.post(...)` 换成了 `client.send_post(...)`。

购物车部分：

```python
cart_response = client.send_post(url=cart_url, data=cart_data, data_type='json')
assert jsonpath.jsonpath(cart_response.json(), '$..msg')[0] == '加入成功', '加入购物车失败'
```

对比原始写法，你会发现调用处清爽了：

- 不用自己写 try/except。
- 不用关心 `requests.post()` 的重复细节。
- 只需要告诉封装方法：地址、数据、请求类型。

## 传 `requests` 还是 `requests.session()`

课堂资料里讲得很重要：

```python
client = ApiClient(requests)          # 每次请求独立
client = ApiClient(requests.session()) # 保持 cookie 会话
```

原因：

- `requests` 模块里有 `.request()`。
- `requests.session()` 对象里也有 `.request()`。
- Python 是动态类型，参数不限定类型。
- 只要调用方式一样，就能用。

## 适合什么场景

- 多个接口有依赖关系：登录、下单、审核流程。
- 重复代码太多：统一封装请求方法。
- 需要切换请求方式：后续只要改封装层。

## 课后自测

1. `**kwargs` 是什么？有什么好处？
2. `**request_data` 和 `request_data` 有什么区别？
3. 为什么 `ApiClient(requests)` 和 `ApiClient(requests.session())` 都能工作？
4. 如果登录成功后返回的是 token，应该怎么传给下一个接口？

---

# 第4节：8月27号，YAML 用例和第一版执行器

## 今天解决什么问题

前面把业务流程写成了 Python 代码。  
但测试同学不一定都会 Python，改用例就要改代码，风险很大。

所以今天开始：

1. 把用例数据写到 YAML 文件里。
2. 用 Python 读取 YAML。
3. 用 pytest 执行 YAML 里的用例。

## 导出和安装依赖

课堂资料里的命令：

```bash
pip freeze > requirements.txt
pip install -r requirements.txt
```

下载慢可以用镜像：

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/
```

注意文件名是 `requirements.txt`，中间有 s。

## YAML 基础

YAML 可以理解成“更接近测试用例的配置格式”。

课堂资料里总结：

- `键: 值` 表示字典。
- 冒号后面要有空格。
- `-` 表示列表。
- 同一层级要左对齐。
- 缩进只能用空格，不能用 Tab。
- `#` 写注释。

示例：

```yaml
name: zhangsan
age: 18
list:
  - 1
  - 2
  - 3
```

对应 Python：

```python
{
    "name": "zhangsan",
    "age": 18,
    "list": [1, 2, 3]
}
```

## 路径转义

Windows 路径里 `\t`、`\n` 可能被 Python 当成转义符。

不推荐：

```python
path = 'F:\test\name.yaml'
```

推荐：

```python
path = r'F:\test\name.yaml'
```

或者：

```python
path = 'F:/test/name.yaml'
```

课堂资料还提醒：

- `./`：当前目录。
- `../`：上一级目录。
- 相对路径看的是运行程序时所在目录，不是只看代码文件放在哪里。

## 第一版 YAML 用例：`8月27号/examples/api-cases-yaml/login.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 商城登录接口
  一级模块: 登录模块
  二级模块: 用户登录
  优先级: P0

用例步骤:
  - 发送登录请求:
      操作类型: 发送请求POST
      请求地址: http://shop-xo.hctestedu.com
      URL参数:
        s: /api/user/login
        application: app
        application_client_type: weixin
      请求头:
        Content-Type: application/x-www-form-urlencoded
      请求数据:
        accounts: <测试账号>
        pwd: "<测试密码>"
        type: username
```

这里的结构是：

```text
基础配置：给测试报告用
用例步骤：告诉框架执行哪些动作
```

`用例步骤` 里是一个列表，列表里有一个字典：

```python
{
    "发送登录请求": {
        "操作类型": "发送请求POST",
        "请求地址": "...",
        ...
    }
}
```

## 读取 YAML：`8月27号/HAT/parse/读取文件.py`

```python
import yaml

from pathlib import Path


PROJECT_DIR = Path(__file__).resolve().parents[2]


def readyaml(load):
    list_data = []

    path = Path(load)
    if not path.is_absolute():
        path = PROJECT_DIR / path

    if path.is_file():
        yaml_files = [path]
    else:
        yaml_files = sorted(path.glob('*.yaml')) + sorted(path.glob('*.yml'))

    if not yaml_files:
        raise FileNotFoundError(f'没有找到 YAML 文件：{path}')

    for yaml_file in yaml_files:
        with yaml_file.open('r', encoding='utf-8') as e:
            data = yaml.safe_load(e)

        if isinstance(data, list):
            list_data.extend(data)
        else:
            list_data.append(data)

    return list_data
```

逐段解释：

- `Path(__file__)`：当前代码文件路径。
- `.resolve()`：转成绝对路径。
- `.parents[2]`：往上找两级，得到项目根目录。
- `path.is_absolute()`：判断是不是绝对路径。
- 如果不是绝对路径，就拼到项目根目录下面。
- 如果传的是文件，就把这个文件放进列表。
- 如果传的是文件夹，就找出所有 `.yaml` 和 `.yml` 文件。
- `yaml.safe_load(e)`：安全读取 YAML。
- 最后统一返回一个列表。

为什么最后要统一返回列表？  
因为 pytest 参数化需要一个列表。列表里有几条数据，就生成几条用例。

## 第一版关键字：`8月27号/HAT/keywords/api_client.py`

```python
class Keywords:
    """requests 二次封装，方法名和 YAML 中的操作类型保持一致"""

    def __init__(self, session):
        self.session = session
```

这里的类名从 `ApiClient` 变成了 `Keywords`，方法名也变成了中文：

- `发送请求POST`
- `发送请求GET`

为什么要保持一致？  
因为 YAML 里写的是：

```yaml
操作类型: 发送请求POST
```

执行器会根据这个名字，去 `Keywords` 类里找同名方法。

方法内部和 8月26号 的 `send_post()` 思路一致，只是参数名变成了中文：

```python
url = kwargs.get("请求地址", None)
data = kwargs.get("请求数据", None)
params = kwargs.get("URL参数", None)
headers = kwargs.get("请求头", None)
files = kwargs.get("文件列表", [])
data_type = kwargs.get("请求类型", "data").lower()
```

这样写的好处是：

- 测试用例里看到的字段名和代码里的参数名一致。
- 测试同学更容易看懂。

## 第一版执行器：`8月27号/HAT/core/testrun.py`

```python
import pytest

import requests

from HAT.keywords.api_client import Keywords
from HAT.parse.读取文件 import readyaml


client = Keywords(requests.session())


class TestRun:
    data = [
        case
        for case in readyaml('./examples/api-cases-yaml')
        if case.get('用例步骤')
    ]

    @pytest.mark.parametrize('data_yaml', data)
    def test_case_yaml(self, data_yaml):
        steps = data_yaml.get('用例步骤', [])
        assert steps, '用例缺少用例步骤'

        for step in steps:
            step_name, step_value = next(iter(step.items()))
            key = step_value.get('操作类型')

            try:
                key_func = client.__getattribute__(key)
            except AttributeError:
                raise AssertionError(f'{step_name} 在 Keywords 中没有找到方法：{key}')

            response = key_func(**step_value)
            print(step_name, response.status_code, response.json())
```

这段代码是执行器的雏形。

### 1. 准备用例数据

```python
data = [
    case
    for case in readyaml('./examples/api-cases-yaml')
    if case.get('用例步骤')
]
```

这是列表推导式：

- 读取文件夹里所有 YAML。
- 只保留有 `用例步骤` 的数据。
- 这样 `test.yaml` 这种语法学习文件会被跳过。

### 2. 参数化

```python
@pytest.mark.parametrize('data_yaml', data)
def test_case_yaml(self, data_yaml):
```

pytest 会遍历 `data`：

- 列表里有 1 条数据，就执行 1 次用例。
- 列表里有 5 条数据，就执行 5 次用例。

### 3. 解析步骤

```python
for step in steps:
    step_name, step_value = next(iter(step.items()))
```

YAML 里每个步骤长这样：

```yaml
- 发送登录请求:
    操作类型: 发送请求POST
```

转换成 Python 是：

```python
{"发送登录请求": {"操作类型": "发送请求POST", ...}}
```

- `step.items()`：取出字典里的键值对。
- `iter(...)`：生成迭代器。
- `next(...)`：取第一个键值对。
- `step_name`：`发送登录请求`。
- `step_value`：`{"操作类型": "发送请求POST", ...}`。

### 4. 找关键字方法

```python
key_func = client.__getattribute__(key)
```

`key` 是 YAML 里的 `操作类型`，例如 `发送请求POST`。  
`client.__getattribute__(key)` 的意思是：从 `client` 对象上找同名方法。

如果找不到，会抛 `AttributeError`，课堂代码把它转成更清楚的提示：

```python
raise AssertionError(f'{step_name} 在 Keywords 中没有找到方法：{key}')
```

### 5. 执行步骤

```python
response = key_func(**step_value)
print(step_name, response.status_code, response.json())
```

注意这里还没有断言。  
今天的目标是先跑通：YAML -> Python 执行器 -> requests。

## 适合什么场景

- 用例内容经常变化。
- 测试同学不想直接改 Python 代码。
- 需要一个简单的关键字驱动框架。

## 课后自测

1. YAML 里的 `用例步骤` 为什么设计成列表？
2. `readyaml()` 为什么最后要返回列表？
3. `next(iter(step.items()))` 在解析什么？
4. `client.__getattribute__(key)` 是怎么把 YAML 和方法连起来的？

---

# 第5节：8月28号，Allure 报告和动态关键字

## 今天解决什么问题

昨天只是控制台输出，测试完成后没有好看的报告，也没有步骤展示。  
今天解决三件事：

1. 生成 Allure 报告。
2. 把 YAML 里的模块、标题、步骤显示到报告里。
3. 让关键字可以扩展到 `key_dir` 目录，不改核心执行器。

## 安装 Allure

课堂资料固定版本：

```bash
pip install allure-pytest==2.13.5 -i https://pypi.tuna.tsinghua.edu.cn/
```

还需要下载 `allure-commandline`，版本是 `2.18.1`。

配置流程：

1. 解压。
2. 把 `allure-commandline` 里面的 `bin` 目录加入环境变量 `Path`。
3. 重启 PyCharm 或终端。
4. 检查：

```bash
allure --version
```

## 运行入口：`8月28号/main.py`

```python
import os

import pytest
from allure_combine import combine_allure

pytest_ars = [
    '-v',
    '-s',
    '--capture=sys',
    '--clean-alluredir',
    '--alluredir=allure-results',
    './HAT/core/testrun.py'
]

pytest.main(pytest_ars)

os.system('allure generate -c -o allure-report')

combine_allure('./allure-report')
```

逐行解释：

- `pytest.main(pytest_ars)`：在 Python 里启动 pytest。
- `-v`：详细模式。
- `-s`：显示 print 输出。
- `--capture=sys`：指定捕获方式。
- `--clean-alluredir`：运行前清空旧报告数据。
- `--alluredir=allure-results`：指定原始测试数据目录。
- `./HAT/core/testrun.py`：指定执行的测试文件。
- `os.system('allure generate -c -o allure-report')`：把原始数据生成 HTML 报告。
- `combine_allure('./allure-report')`：生成一个 `complete.html`，方便直接打开。

课堂资料还提到两个命令：

```bash
allure open allure-report
allure serve allure-results
```

- `allure open`：打开已生成的报告。
- `allure serve`：临时预览报告。

## 报告里的基础配置

YAML 里写了：

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 商城登录接口
  一级模块: 登录模块
  二级模块: 用户登录
  优先级: P0
```

执行器里读取这些数据：

```python
base_info = data_yaml.get('基础配置', {})

allure.dynamic.parameter('caseinfo', '')
allure.dynamic.feature(base_info.get('一级模块', '默认模块'))
allure.dynamic.story(base_info.get('二级模块', '默认模块'))
allure.dynamic.title(base_info.get('用例标题', '默认用例标题'))
```

解释：

- `feature`：一级模块。
- `story`：二级模块。
- `title`：报告里的用例标题。
- `.get(..., '默认模块')`：如果 YAML 里没写，就给默认值，避免报告字段缺失。

## 步骤展示：`@allure.step`

在请求方法上加装饰器：

```python
@allure.step("发送请求POST")
def 发送请求POST(self, **kwargs):
    ...
```

在执行器里再加一层：

```python
with allure.step(step_name):
    response = key_func(**step_value)
```

这样报告里会出现类似：

```text
发送登录请求
    发送请求POST
```

为什么有两层？  
因为第一层是“业务步骤名”，第二层是“关键字方法名”。报告里能同时看到“做了什么事”和“用了哪个关键字”。

## 进度条 tqdm

安装：

```bash
pip install tqdm==4.67.3 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

代码：

```python
from tqdm import tqdm

with tqdm(total=len(steps), desc='开始执行') as pbar:
    for step in steps:
        step_name, step_value = next(iter(step.items()))
        pbar.set_description(f'当前步骤:{step_name}')
        pbar.update(1)

        with allure.step(step_name):
            response = key_func(**step_value)
```

解释：

- `total=len(steps)`：总步骤数。
- `set_description()`：修改进度条前面的文字。
- `update(1)`：每执行完一步，进度加 1。

## 动态关键字：`8月28号/HAT/key_dir/发送请求POST.py`

课堂资料里的规则：

```text
文件名 = 操作类型
类名 = 操作类型
方法名 = 操作类型
```

文件 `发送请求POST.py` 里的类也叫 `发送请求POST`，方法也叫 `发送请求POST`。

核心代码：

```python
import allure


class 发送请求POST:
    def __init__(self, request):
        self.request = request

    @allure.step("发送请求POST")
    def 发送请求POST(self, **kwargs):
        request_data = {
            "url": kwargs.get("请求地址", None),
            "params": kwargs.get("URL参数", None),
            "headers": kwargs.get("请求头", None),
            "files": kwargs.get("文件列表", []),
        }

        data = kwargs.get("请求数据", None)
        data_type = kwargs.get("请求类型", "data").lower()

        if data_type == "json":
            request_data["json"] = data
        elif data_type == "data":
            request_data["data"] = data
        else:
            raise Exception("请求类型错误，仅支持json/data")

        response = self.request.request("post", **request_data)
        return response
```

执行器里的加载逻辑：

```python
import sys

try:
    key_func = client.__getattribute__(key)
except AttributeError:
    sys.path.append(str(KEY_DIR))

    module = __import__(key)
    class_ = getattr(module, key)
    key_func = class_(requests).__getattribute__(key)
```

这段逻辑的意思是：

1. 先在 `Keywords` 类里找方法。
2. 如果找不到，就去 `HAT/key_dir` 目录找同名文件。
3. `__import__(key)`：导入同名的模块。
4. `getattr(module, key)`：获取同名类。
5. `class_(requests)`：创建对象。
6. `.__getattribute__(key)`：获取同名方法。

这样做的好处是：

- 同事可以自己扩展关键字。
- 不用每次都改核心执行器。

## 适合什么场景

- 需要给领导或开发看测试报告。
- 用例步骤需要清晰展示。
- 多人协作时，不同人扩展不同关键字。

## 课后自测

1. `allure-results` 和 `allure-report` 有什么区别？
2. `@allure.step` 加在哪里？
3. 动态关键字为什么要求文件名、类名、方法名都一致？

---
# 第6节：8月29号，全局变量和变量渲染

## 今天解决什么问题

前面你已经能把接口步骤写进 YAML 了，但马上会遇到一个新问题：

```text
URL 不想写死
token 也不能写死
测试环境换域名时，不想一个文件一个文件地改
```

所以今天要做两件事：

1. 建一个“全局变量容器”：`g_context`。
2. 做一个“变量渲染器”：`refresh`，把 YAML 里的 `{{URL}}` 变成真实值。

可以先把这个过程记成：

```text
context.yaml 里的变量 -> 放进 g_context -> 渲染 YAML 里的 {{变量}} -> 执行接口步骤
```

---

## 第 1 块：全局变量容器 g_context

源码：`8月29号/HAT/core/globalContext.py`

```python
class g_context:
    """全局变量容器，_dic 是类变量，用于多处共享数据"""

    _dic = {}

    def set_dict(self, key, value):
        self._dic[key] = value

    def set_by_dict(self, dic):
        self._dic.update(dic)

    def get_dict(self, key):
        return self._dic.get(key, None)

    def show_dict(self):
        return self._dic
```

### 这段代码到底在干嘛

把它想象成一个“公共储物柜”。

```text
g_context._dic 就是柜子本体
set_dict()：把某个变量放进柜子
set_by_dict()：一次性把一批变量放进柜子
get_dict()：按名字取变量
show_dict()：看柜子里现在有什么
```

### 重点：`_dic` 为什么写在类里面，而不是写在 `__init__` 里

这里是一个很重要的 Python 概念。

```python
_dic = {}
```

它写在类里面、方法外面，所以叫“类变量”。

类变量的特点是：所有 `g_context()` 对象都用同一份 `_dic`。

所以不管代码在哪个文件里执行：

```python
g_context().set_dict('URL', 'http://xxx')
```

只要后面再写：

```python
g_context().get_dict('URL')
```

都能拿到刚才的值。

这就是课程里用 `g_context()` 做全局变量的原因：跨文件、跨用例、跨步骤共享数据。

### 为什么方法名里有 set、get、show

因为外部代码不直接乱改 `_dic`，而是通过方法来操作。

这样写的好处是：

- 语义清楚：`set_dict` 就是设置，`get_dict` 就是取值。
- 以后想加校验、日志、类型检查，只要改方法内部。
- 不用每个地方都自己写 `_dic['xxx'] = xxx`。

---

## 第 2 块：读取 context.yaml

源码：`8月29号/HAT/parse/YamlCaseParser.py`

```python
import json
from pathlib import Path

import yaml

from HAT.core.globalContext import g_context


PROJECT_DIR = Path(__file__).resolve().parents[2]


def readYaml(file_path):
    path = Path(file_path)
    if not path.is_absolute():
        path = PROJECT_DIR / path

    with path.open('r', encoding='utf-8') as file:
        data = yaml.safe_load(file)

    return [data] if not isinstance(data, list) else data


def load_context_from_yaml(folder_path):
    path = Path(folder_path)
    if not path.is_absolute():
        path = PROJECT_DIR / path

    yaml_file_path = path / 'context.yaml'
    with yaml_file_path.open('r', encoding='utf-8') as file:
        data = yaml.safe_load(file)

    if data:
        g_context().set_by_dict(data)

    print('全局变量', g_context().show_dict())
```

### `PROJECT_DIR` 是什么

```python
PROJECT_DIR = Path(__file__).resolve().parents[2]
```

这句话是在找当前 Python 文件的位置。

```text
HAT/parse/YamlCaseParser.py
```

`parents[0]` 是 `HAT/parse`，`parents[1]` 是 `HAT`，`parents[2]` 就是当天课程项目根目录。

这样做是为了防止你从不同目录运行项目时找不到文件。

### `Path` 好在哪里

普通字符串拼接路径容易出问题：

```text
Windows 里常用反斜杠 \
Linux/Mac 里常用正斜杠 /
```

`pathlib.Path` 会自动帮我们处理路径分隔符。

```python
path = PROJECT_DIR / path
```

这里的 `/` 不是做除法，是 `Path` 提供的“拼接路径”写法。

### `yaml.safe_load` 为什么重要

```python
data = yaml.safe_load(file)
```

YAML 文件会被读取成 Python 的字典或列表。

比如：

```yaml
URL: http://shop-xo.hctestedu.com
```

读取后就是：

```python
{'URL': 'http://shop-xo.hctestedu.com'}
```

`safe_load` 表示用安全方式解析。课堂资料里用的是 `yaml.safe_load`，写代码时也应该优先用它，因为它能避免执行 YAML 里不安全的对象。

### `load_context_from_yaml` 的流程

```text
1. 拼出 context.yaml 的路径
2. 打开并解析 YAML
3. 如果里面有数据，就放进 g_context
4. 打印当前全局变量，方便课堂调试
```

### context.yaml 示例

源文件：`8月29号/examples/api-cases-yaml/context.yaml`

```yaml
URL: http://shop-xo.hctestedu.com
```

它只有一个变量 `URL`。以后环境变化时，只需要改这个文件。

---

## 第 3 块：变量渲染器 refresh

源码：`8月29号/HAT/utils/VarRender.py`

```python
import json

from jinja2 import Template


def refresh(target, context):
    """把数据中的 {{变量名}} 替换成全局变量里的真实值"""
    if target is None:
        return None

    if isinstance(target, (dict, list)):
        target_str = json.dumps(target, ensure_ascii=False)
    else:
        target_str = str(target)

    return Template(target_str).render(context)
```

### 它解决什么问题

YAML 里写的是：

```yaml
请求地址: '{{URL}}'
```

程序真正发请求时不能发 `'{{URL}}'`，要先变成：

```text
http://shop-xo.hctestedu.com
```

`refresh()` 就是这个“把变量变成真实值”的动作。

### `isinstance(target, (dict, list))` 是干嘛的

```python
if isinstance(target, (dict, list)):
    target_str = json.dumps(target, ensure_ascii=False)
else:
    target_str = str(target)
```

`isinstance` 用来判断一个东西是不是某种类型。

课程里的 `step_value` 通常是一个字典，比如：

```python
{
    '操作类型': '发送请求POST',
    '请求地址': '{{URL}}'
}
```

但 `jinja2.Template()` 主要处理字符串，所以要把字典先变成字符串。

```python
json.dumps(target, ensure_ascii=False)
```

这句会把字典转成 JSON 字符串。

`ensure_ascii=False` 很重要，它让中文保持中文，而不是变成 `\\uXXXX` 这种形式。

### `Template(...).render(context)` 是什么

这是 `jinja2` 的模板语法。

```python
Template('请求地址：{{URL}}').render({'URL': 'http://xxx'})
```

执行后：

```text
请求地址：http://xxx
```

`{{ }}` 是 jinja2 的占位符写法，和 YAML 本身的字典语法没关系。

### 注意一个关键点

`refresh()` 返回的是字符串，不是字典。

即使你传进去的是字典，中间也先变成了：

```text
'{"操作类型": "发送请求POST", "请求地址": "http://xxx"}'
```

后面执行器里要用 `json.loads()` 把它再变回字典。

---

## 第 4 块：执行器第一次使用全局变量和渲染

源码：`8月29号/HAT/core/TestRunner.py`

```python
import json

import allure
import pytest
import requests

from HAT.keywords.api_client import Keywords
from HAT.core.globalContext import g_context
from HAT.parse.YamlCaseParser import load_context_from_yaml, readYaml
from HAT.utils.VarRender import refresh


load_context_from_yaml('examples/api-cases-yaml')
data = readYaml('examples/api-cases-yaml/login.yaml')


class TestRunner:
    @pytest.mark.parametrize('caseinfo', data)
    def test_case_exceute(self, caseinfo):
        keywords = Keywords(requests)
        base_info = caseinfo.get('基础配置', {})

        allure.dynamic.parameter('caseinfo', '')
        allure.dynamic.feature(base_info.get('一级模块', '默认模块'))
        allure.dynamic.story(base_info.get('二级模块', '默认模块'))
        allure.dynamic.title(base_info.get('用例标题', '默认用例标题'))

        steps = caseinfo.get('用例步骤', [])
        assert steps, '用例缺少用例步骤'

        for step in steps:
            step_name, step_value = next(iter(step.items()))
            print('没有渲染之前的用例数据', step_value)

            context = g_context().show_dict()
            rendered = refresh(step_value, context)
            step_value = json.loads(rendered)
            print('渲染之后的用例数据', step_value)

            key = step_value['操作类型']
            key_func = keywords.__getattribute__(key)
            response = key_func(**step_value)
            print('发送结果', response.status_code, response.json())
```

### 顶部这两句很关键

```python
load_context_from_yaml('examples/api-cases-yaml')
data = readYaml('examples/api-cases-yaml/login.yaml')
```

它们发生在类定义阶段：

```text
第 1 句：先加载全局变量
第 2 句：再读用例文件
```

所以后面测试方法执行时，`g_context` 里已经有 `URL` 了。

### `next(iter(step.items()))`

YAML 里每个步骤长这样：

```yaml
- 发送登陆接口:
    操作类型: 发送请求POST
```

读成 Python 后是：

```python
[{'发送登陆接口': {'操作类型': '发送请求POST'}}]
```

每个 `step` 是一个只有一个键值对的字典。

```python
step_name, step_value = next(iter(step.items()))
```

意思是：从这个字典里取出唯一的一对键值。

```text
step_name = 发送登陆接口
step_value = {'操作类型': '发送请求POST'}
```

这里为什么要用 `next(iter(...))`？  
因为代码想表达“一个步骤名称只对应一份步骤数据”，不关心字典里多个键的情况。

### 渲染这段

```python
context = g_context().show_dict()
rendered = refresh(step_value, context)
step_value = json.loads(rendered)
```

流程是：

```text
step_value 是字典
refresh() 先把字典转成字符串，再把 {{URL}} 替换成真实值
json.loads() 再把字符串变回字典
```

这就是为什么要三个步骤连在一起。

### `keywords.__getattribute__(key)`

YAML 里写：

```yaml
操作类型: 发送请求POST
```

代码里有同名方法：

```python
def 发送请求POST(self, **kwargs):
```

所以：

```python
key = '发送请求POST'
key_func = keywords.__getattribute__(key)
```

就相当于：

```python
key_func = keywords.发送请求POST
```

注意这里没有加括号，所以只是拿到方法，还没执行。

### `key_func(**step_value)`

`step_value` 是一个字典，里面有：

```python
{
    '操作类型': '发送请求POST',
    '请求地址': 'http://xxx',
    ...
}
```

`**step_value` 会把字典拆成关键字参数传给方法。

这就是后面关键字方法能接收 `**kwargs` 的原因。

---

## 第 5 块：YAML 里的占位符

源文件：`8月29号/examples/api-cases-yaml/login.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 商城登录用例标题
  一级模块: 登录模块
  二级模块: 登录功能

用例步骤:
  - 发送登陆接口:
      操作类型: 发送请求POST
      请求地址: '{{URL}}'
      URL参数:
        s: /api/user/login
        application: app
        application_client_type: weixin
      请求头:
        Content-Type: application/x-www-form-urlencoded
      请求数据:
        accounts: <测试账号>
        pwd: "<测试密码>"
        type: username
```

这里重点是：

```yaml
请求地址: '{{URL}}'
```

它没有直接写域名，而是用了 `{{URL}}`。

真实值来自：

```yaml
URL: http://shop-xo.hctestedu.com
```

---

## 为什么这样设计

如果没有变量渲染，YAML 会长这样：

```yaml
请求地址: http://shop-xo.hctestedu.com
```

看起来也能用，但缺点很大：

```text
换测试环境，要改很多 YAML。
URL 拼错，要一个文件一个文件找。
以后 token、用户名、数据库信息都会重复出现。
```

有了 `context.yaml + g_context + refresh`，就能做到：

```text
环境信息集中管理
用例只关心业务步骤
一个地方改，所有用例生效
```

---

## 适合什么场景

- 多环境测试：测试环境、预发布环境地址不同。
- 登录后的接口：后面要从响应里提取 token。
- 公共参数：比如 `application`、`application_client_type`。
- 团队协作：测试同学改 YAML 就能写用例，不需要都改 Python。

---

## 课后自测

1. `g_context` 里的 `_dic` 如果改成写在 `__init__` 里，会影响什么？
2. `refresh()` 为什么要把字典先转成字符串？
3. `json.loads()` 在执行器里解决什么问题？
4. `yaml.safe_load` 和普通读取文件有什么区别？
5. 如果你在 YAML 里写了 `{{HOST}}`，但 `context.yaml` 里没有 `HOST`，程序会发生什么？

---

# 第7节：8月30号，前置脚本、后置脚本和 local_context

## 今天解决什么问题

有些数据不想写死在 YAML 里，比如：

```text
今天造一个临时用户名
登录前要初始化一些变量
用例结束后清理某些数据
```

所以今天给用例加了三个能力：

```text
local_context：本条用例自己的变量
前置脚本：用例开始前执行
后置脚本：用例结束后执行
```

今天执行器变得更像一个“测试用例调度器”了。

---

## 第 1 块：前置脚本执行器

源码：`8月30号/HAT/extend/script/run_script.py`

```python
def exec_script(script, context):
    if script is None:
        return

    exec(script, {'context': context})
```

### 这段代码很短，但很关键

它接受两个东西：

```text
script：一段 Python 字符串
context：一个字典，通常是全局变量
```

然后：

```python
exec(script, {'context': context})
```

`exec()` 会把字符串当代码执行。

举个例子，YAML 里写：

```yaml
前置脚本:
  - "context.update({'name': 'youer'})"
```

读到 Python 里就是字符串：

```python
"context.update({'name': 'youer'})"
```

`exec()` 执行它，相当于临时执行了这句：

```python
context.update({'name': 'youer'})
```

因为 `exec()` 的第二个参数里指定了 `context`，所以字符串里的 `context` 能找到全局变量字典。

### 为什么把 `context` 传进去

如果直接写：

```python
exec(script)
```

字符串里访问不到 `context`。

所以课程里用：

```python
exec(script, {'context': context})
```

这相当于给这段字符串代码准备了一个运行环境。

---

## 第 2 块：api_keywords 里的请求关键字

源码：`8月30号/HAT/keywords/api_keywords.py`

这天的 `Keywords` 类已经把 GET、POST 请求封装好了。

核心结构是：

```python
import allure
import requests


class Keywords:
    def __init__(self, request):
        self.request = request

    @allure.step('发送请求POST')
    def 发送请求POST(self, **kwargs):
        ...
```

这里重点先理解三个点。

### 1. `__init__(self, request)`

```python
keywords = Keywords(requests)
```

把 `requests` 这个模块传进对象里。

后面方法里用：

```python
self.request.request('post', **requests_data)
```

这样做的好处是：

- 用 `requests` 时传 `requests`
- 用 `requests.session()` 时传 session 对象
- 关键字代码不用大改

这是为后面 Session 复用做准备的。

### 2. `**kwargs`

YAML 里的参数名很灵活，可能传：

```yaml
请求地址
URL参数
请求头
请求数据
请求类型
文件列表
```

如果方法写成：

```python
def 发送请求POST(self, 请求地址, URL参数, 请求头, 请求数据):
```

以后每加一个参数都要改方法签名。

课程里写成：

```python
def 发送请求POST(self, **kwargs):
```

`**kwargs` 会把所有关键字参数收成一个字典。

### 3. 请求类型分流

关键字里有一段：

```python
data_type = kwargs.get('请求类型', 'data').lower()

requests_data = {
    'url': url,
    'params': params,
    'headers': headers,
    'files': files,
}

if data_type == 'json':
    requests_data['json'] = data
elif data_type == 'data':
    requests_data['data'] = data
else:
    raise Exception('请求类型错误，仅支持json/data')
```

因为 `requests` 发请求时：

```python
requests.post(..., data=xxx)
```

和：

```python
requests.post(..., json=xxx)
```

效果不同。

所以关键字统一用“请求类型”来决定最终用 `data=` 还是 `json=`。

---

## 第 3 块：执行器的主流程

源码：`8月30号/HAT/core/TestRunner.py`

先看整体流程：

```text
1. 读取 context.yaml，加载全局变量
2. 读取用例 YAML
3. 每条用例开始前：
   - 拷贝全局变量
   - 合并 local_context
   - 渲染前置脚本
   - 执行前置脚本
4. 循环执行用例步骤
5. 每条用例结束后执行后置脚本
```

### 第 4 步的代码

```python
steps = caseinfo.get('用例步骤', None)
with tqdm(total=len(steps), desc='开始执行') as pbar:
    for step in steps:
        step_name = list(step.keys())[0]
        step_value = list(step.values())[0]
        print('没有渲染之前的用例数据', step_value)
        pbar.set_description(f'{base_info.get("用例标题")}-当前步骤:{step_name}')
        pbar.update(1)

        with allure.step(step_name):
            key = step_value['操作类型']
            context = copy.deepcopy(g_context().show_dict())
            step_value = eval(refresh(step_value, context))
            print('渲染之后的用例数据', step_value)

            try:
                key_func = keywords.__getattribute__(key)
            except AttributeError:
                sys.path.append('./HAT/key_dir')
                module = __import__(key)
                class_ = getattr(module, key)
                key_func = class_(requests).__getattribute__(key)
            except Exception as e:
                raise e

            key_func(**step_value)
```

### `copy.deepcopy`

```python
context = copy.deepcopy(g_context().show_dict())
```

`deepcopy` 是深拷贝，会把嵌套字典也复制一份。

为什么要拷贝？

因为 `refresh()` 只应该“读取”变量，不应该顺手修改全局变量。

比如：

```text
g_context 里有嵌套配置
如果 refresh 过程中改了里面的值
可能影响后面的步骤
```

深拷贝等于拿了一份草稿纸，不在试卷上乱写。

### `eval(refresh(step_value, context))`

这里比 8 月 29 号多了一步 `eval()`。

8 月 29 号的写法是：

```python
step_value = json.loads(rendered)
```

8 月 30 号的写法是：

```python
step_value = eval(refresh(step_value, context))
```

`eval()` 会把符合 Python 字面量格式的字符串解析成 Python 对象。

比如：

```python
eval("{'name': 'youer'}")
```

会变成：

```python
{'name': 'youer'}
```

课程用 `eval()` 的原因是：`refresh()` 返回字符串，而 `step_value` 需要变回字典。

### 动态关键字查找

```python
try:
    key_func = keywords.__getattribute__(key)
except AttributeError:
    sys.path.append('./HAT/key_dir')
    module = __import__(key)
    class_ = getattr(module, key)
    key_func = class_(requests).__getattribute__(key)
except Exception as e:
    raise e
```

逻辑是：

```text
1. 先在 Keywords 类里找同名方法
2. 如果找不到，去 HAT/key_dir 目录找同名 Python 文件
3. 导入同名模块
4. 找同名类
5. 找同名方法
```

这就是“关键字可以扩展”的意思。

---

## 第 4 块：前置脚本在 YAML 里怎么写

源文件：`8月30号/examples/api-cases-yaml/login.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 商城登录用例标题
  一级模块: 登录模块
  二级模块: 登录功能

前置脚本:
  - "context.update({'name': 'youer'})"
后置脚本: ""

用例步骤:
  - 发送登陆接口:
      操作类型: 发送请求POST
      请求地址: '{{URL}}'
      URL参数:
        s: /api/user/login
        application: app
        application_client_type: weixin
      请求数据:
        accounts: '{{name}}'
        pwd: '<测试密码>'
        type: username
```

流程是：

```text
前置脚本执行后，g_context 里多了 name 变量
YAML 里的 {{name}} 被渲染成真实值
接口拿到真实参数
```

---

## 第 5 块：执行顺序要牢牢记住

这条用例的完整顺序是：

```text
1. context.yaml 加载全局变量
2. 读取 login.yaml
3. 执行前置脚本：g_context 里增加 name
4. 渲染请求地址：{{URL}}
5. 渲染账号：{{name}}
6. 发送 POST 请求
7. 执行后置脚本：这里后置脚本是空字符串，所以跳过
```

如果一条用例有多个步骤，前置脚本不会重复执行，它是在所有步骤之前执行一次。

---

## 为什么需要前置和后置脚本

很多接口测试不是“发一次请求”就结束，而是有生命周期。

比如：

```text
前置：准备测试账号、准备 token、准备数据库状态
步骤：发请求
后置：删除测试数据、还原环境、清理临时变量
```

课程把这些动作放在 YAML 里，好处是：

```text
用例自己决定要准备什么
不用把所有准备逻辑都写死在执行器里
测试同学可以按用例定制
```

---

## 特别提醒：eval 和 exec 的风险

`eval()` 和 `exec()` 很强大，但也很危险。

它们会执行字符串里的代码。如果 YAML 内容来自不可信的人或系统，就可能执行恶意代码。

在这套课程框架里，YAML 是测试团队自己维护的，所以课堂资料这样演示是可以理解的。  
但你要记住：企业级框架里通常会限制这类能力，或者做权限、白名单、沙箱控制。

这属于以后学习“框架安全设计”的伏笔。

---

## 适合什么场景

- 登录前准备账号变量。
- 每条用例需要独立的临时数据。
- 用例开始前初始化状态，结束后清理状态。
- 不想把准备动作写死在 Python 里，希望测试同学在 YAML 里控制。

---

## 课后自测

1. `exec(script, {'context': context})` 里的第二个参数有什么作用？
2. 前置脚本执行一次还是每个步骤都执行？
3. `copy.deepcopy()` 和普通赋值有什么区别？
4. 为什么 `requests` 发请求要区分 `data=` 和 `json=`？
5. 如果前置脚本里写错了 Python 语法，会在什么阶段报错？

---

# 第8节：8月31号，加载多个 YAML 文件和接口关联

## 今天解决什么问题

真实项目里不会只有一个登录接口。

常见流程是：

```text
登录 -> 拿 token -> 用 token 去加购物车 -> 下单
```

所以今天要解决：

1. 怎么自动读取一个文件夹里的多个 YAML 文件？
2. 怎么保证文件执行顺序？
3. 怎么从前一个接口响应里提取 token？
4. 怎么在下一个接口里使用 token？

这就是“接口关联”。

---

## 第 1 块：按编号加载多个 YAML 文件

源码：`8月31号/HAT/parse/YamlCaseParser.py`

```python
import os

import yaml

from HAT.core.globalContext import g_context


def readYaml(file_path):
    case_info = []
    with open(file_path, 'r', encoding='utf-8') as f:
        data = yaml.load(f, Loader=yaml.FullLoader)
        case_info.append(data)
    return case_info


def load_context_from_yaml(file_path):
    yaml_file_path = os.path.join(file_path, 'context.yaml')
    with open(yaml_file_path, 'r', encoding='utf-8') as file:
        data = yaml.load(file, Loader=yaml.FullLoader)
        if data:
            g_context().set_by_dict(data)
        print('全局变量', g_context().show_dict())


def load_yaml_files(file_path):
    yaml_case_infos = []
    suite_folder = os.path.join(file_path)
    load_context_from_yaml(file_path)

    file_names = [
        (int(f.split('_')[0]), f)
        for f in os.listdir(suite_folder)
        if f.endswith('.yaml') and f.split('_')[0].isdigit()
    ]
    file_names.sort()
    file_names = [f[-1] for f in file_names]

    for file_name in file_names:
        file_path = os.path.join(suite_folder, file_name)
        with open(file_path, 'r', encoding='utf-8') as file:
            caseinfo = yaml.full_load(file)
            yaml_case_infos.append(caseinfo)

    return yaml_case_infos
```

### 重点：文件命名规则

课程要求用例文件名像这样：

```text
1_login.yaml
2_addcard.yaml
3_xxx.yaml
```

代码里的核心判断：

```python
f.endswith('.yaml') and f.split('_')[0].isdigit()
```

意思是：

```text
文件必须以 .yaml 结尾
文件名第一段必须是数字
```

比如：

```text
1_login.yaml   -> 读取
2_addcard.yaml -> 读取
readme.yaml    -> 不读取
test_1.yaml    -> 不读取
```

### 为什么要编号

因为接口之间有依赖关系。

```text
先登录
再使用 token
所以 1_login.yaml 要在 2_addcard.yaml 前面执行
```

### 代码怎么排序

```python
file_names = [
    (int(f.split('_')[0]), f)
    for f in os.listdir(suite_folder)
    if f.endswith('.yaml') and f.split('_')[0].isdigit()
]
file_names.sort()
```

这是列表推导式。

假设文件夹里有：

```text
1_login.yaml
2_addcard.yaml
```

第一轮 `file_names` 是：

```python
[(1, '1_login.yaml'), (2, '2_addcard.yaml')]
```

`sort()` 会按第一个元素排序，也就是数字 1、2。

然后：

```python
file_names = [f[-1] for f in file_names]
```

取回文件名：

```python
['1_login.yaml', '2_addcard.yaml']
```

### 注意 `load_yaml_files` 里也加载了全局变量

```python
load_context_from_yaml(file_path)
```

所以执行器只要调用 `load_yaml_files()`，就会自动加载 `context.yaml`。

---

## 第 2 块：把响应对象放进全局变量

源码：`8月31号/HAT/keywords/api_keywords.py`

在 `发送请求POST` 里新增了这句：

```python
g_context().set_dict('响应结果', response)
```

完整逻辑片段：

```python
response = self.request.request('post', **requests_data)
print('发送结果', response.json())
g_context().set_dict('响应结果', response)
return response
```

这里要注意：

```text
g_context 里保存的是 response 对象
不是 response.json()
```

两者区别：

```text
response：完整的响应对象，可以拿状态码、响应头、响应体
response.json()：把响应体解析成 Python 字典或列表
```

保存对象的好处是后面既可以用 JSON，也可以扩展用状态码、响应头。

---

## 第 3 块：提取数据JSON

源码：`8月31号/HAT/keywords/api_keywords.py`

```python
@allure.step('提取数据JSON')
def 提取数据JSON(self, **kwargs):
    expression = kwargs.get('表达式', None)
    index = kwargs.get('下标', 0)
    if index is None:
        index = 0

    response = g_context().get_dict('响应结果').json()
    result = jsonpath.jsonpath(response, expression)
    if not result:
        raise Exception(f'没有找到对应的数据:{expression}')

    ex_data = result[index]
    g_context().set_dict(kwargs['变量名'], ex_data)
    print('全局变量中有没有token值', g_context().show_dict())
```

### 这段代码的业务含义

比如登录接口返回：

```json
{
  "msg": "登录成功",
  "data": {
    "token": "abcdefg123"
  }
}
```

YAML 里想提取 token：

```yaml
- 提取响应数据token:
    操作类型: 提取数据JSON
    表达式: $..token
    下标: 0
    变量名: my_token
```

流程就是：

```text
1. 从 g_context 拿响应结果
2. 用 jsonpath 找 token
3. 找到后保存成 my_token
4. 后面的 YAML 就能写 {{my_token}}
```

### `jsonpath` 的返回值

`jsonpath` 库的结果通常是列表。

比如：

```text
$..token
```

可能匹配到：

```python
['abcdefg123']
```

所以要用：

```python
result[index]
```

或者默认：

```python
result[0]
```

课程里已经讲过：`jsonpath` 的结果要注意取 `[0]`。

如果找不到，`jsonpath` 通常返回 `False`，所以这里判断：

```python
if not result:
    raise Exception(...)
```

这样能在用例里明确报“没有找到数据”，而不是后面莫名其妙报错。

---

## 第 4 块：登录文件

源文件：`8月31号/examples/api-cases-yaml/1_login.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 商城登录用例标题
  一级模块: 登录模块
  二级模块: 登录功能

前置脚本:
  - "context.update({'name': 'youer'})"
后置脚本: ""

用例步骤:
  - 发送登陆接口:
      操作类型: 发送请求POST
      请求地址: '{{URL}}'
      URL参数:
        s: /api/user/login
        application: app
        application_client_type: weixin
      请求数据:
        accounts: '{{name}}'
        pwd: '<测试密码>'
        type: username

  - 提取响应数据token:
      操作类型: 提取数据JSON
      表达式: $..token
      下标: 0
      变量名: my_token
```

这条用例做了两件事：

```text
1. 登录
2. 提取 token，保存成 my_token
```

---

## 第 5 块：加购物车文件

源文件：`8月31号/examples/api-cases-yaml/2_addcard.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 购物车用例标题
  一级模块: 购物车模块
  二级模块: 购物车功能

用例步骤:
  - 发送购物车接口:
      操作类型: 发送请求POST
      请求地址: '{{URL}}'
      URL参数:
        s: api/cart/save
        application: app
        application_client_type: weixin
        token: '{{my_token}}'
      请求数据:
        goods_id: '2'
        stock: '1'
```

重点在这一行：

```yaml
token: '{{my_token}}'
```

`my_token` 来自前一条文件。

所以整个链条是：

```text
1_login.yaml 登录 -> 提取 token -> 放进 g_context
2_addcard.yaml 渲染 {{my_token}} -> 发请求
```

这就是接口关联的核心。

---

## 执行器这天的变化

`8月31号/HAT/core/TestRunner.py` 里从：

```python
data = readYaml(...)
```

变成了：

```python
data = load_yaml_files(...)
```

也就是说，执行器不再只读一个固定文件，而是按编号读一整套 YAML。

后面的执行流程和昨天一致：

```text
前置脚本 -> 步骤循环 -> 后置脚本
```

---

## 为什么接口关联很重要

真实接口很少有完全独立的。

比如：

```text
没有 token，购物车接口可能失败
没有订单 id，订单详情接口无法测
没有商品 id，添加购物车接口没有意义
```

自动化里如果每次都手工复制 token，就失去意义了。

所以要把“前一个接口的输出”自动变成“后一个接口的输入”。

---

## 适合什么场景

- 登录后操作个人中心、购物车、订单。
- 创建资源后查看、修改、删除资源。
- 数据库查询出 id 后，再调用接口。
- 多接口链路回归测试。

---

## 课后自测

1. `load_yaml_files()` 为什么要求文件名以数字开头？
2. `g_context` 里保存 `response` 和保存 `response.json()` 有什么区别？
3. `jsonpath.jsonpath()` 找不到数据时，代码靠什么及时发现？
4. `{{my_token}}` 是在什么时候被替换成真实值的？
5. 如果 `1_login.yaml` 执行失败，`2_addcard.yaml` 里的 `{{my_token}}` 会怎样？

---

# 第9节：9月1号，断言关键字和完整框架结构

## 今天解决什么问题

以前你只是发请求、提取数据，但程序没有明确判断“结果到底对不对”。

人眼看响应是手工测试，程序判断才是自动化测试。

所以今天补上自动化最重要的能力：

```text
断言
```

同时，课程项目的整体结构也基本成型了。

---

## 先看今天的完整框架链路

到这里，你应该能看懂这条主链路：

```text
main.py
  -> pytest.main()
    -> HAT/core/TestRunner.py
      -> HAT/parse/YamlCaseParser.py
        -> 读取 context.yaml 和业务用例 YAML
      -> HAT/utils/VarRender.py
        -> 渲染 {{变量}}
      -> HAT/keywords/api_keywords.py
        -> 发请求、提取数据、断言
      -> HAT/core/globalContext.py
        -> 全局变量
      -> allure / pytest
        -> 报告
```

你可以把执行器理解成“导演”，关键字理解成“演员”，YAML 理解成“剧本”。

---

## 第 1 块：断言的核心方法

源码：`9月1号/HAT/keywords/api_keywords.py`

```python
@allure.step("断言文本")
def 断言文本(self, **kwargs):
    comparators = {
        "==": lambda a, b: a == b,
        ">=": lambda a, b: a >= b,
        "<=": lambda a, b: a <= b,
        "!=": lambda a, b: a != b,
        ">": lambda a, b: a > b,
        "<": lambda a, b: a < b,
        "in": lambda a, b: a in b,
    }

    message = kwargs.get("错误信息", None)
    operators = kwargs.get("比较符", '==')
    compare_type = kwargs.get("断言类型", "文本")

    if operators not in comparators:
        raise Exception(f"没有对应的比较符{operators}")

    if compare_type == "数字":
        kwargs["期望结果"] = float(kwargs["期望结果"])
    else:
        kwargs["期望结果"] = str(kwargs["期望结果"])

    if not comparators[operators](kwargs["实际结果"], kwargs["期望结果"]):
        if message:
            raise Exception(message)
        else:
            raise Exception(f"{kwargs['实际结果']} {operators} {kwargs['期望结果']}失败")
```

### 第一步：准备比较器

```python
comparators = {
    "==": lambda a, b: a == b,
    ...
}
```

`lambda a, b: a == b` 是一个匿名函数。

它等价于：

```python
def equal(a, b):
    return a == b
```

课程里把“比较符”和“比较动作”放进字典。

好处是：

```text
用户写 ==，程序就知道执行 a == b
用户写 >，程序就知道执行 a > b
```

这比写一长串 `if operators == '==': ... elif operators == '>': ...` 更好维护。

### 第二步：拿参数

```python
message = kwargs.get("错误信息", None)
operators = kwargs.get("比较符", '==')
compare_type = kwargs.get("断言类型", "文本")
```

对应 YAML 里可能这样写：

```yaml
操作类型: 断言文本相等
期望结果: 登录成功
实际结果: '{{my_msg}}'
错误信息: 登录结果不对
```

如果没有传“比较符”，默认用 `==`。  
如果没有传“断言类型”，默认按“文本”处理。

### 第三步：校验比较符

```python
if operators not in comparators:
    raise Exception(f"没有对应的比较符{operators}")
```

这是防止用户写了不支持的符号，比如 `===`。

程序应该尽早报错，而不是后面悄悄得出错误结果。

### 第四步：类型转换

```python
if compare_type == "数字":
    kwargs["期望结果"] = float(kwargs["期望结果"])
else:
    kwargs["期望结果"] = str(kwargs["期望结果"])
```

为什么？

因为 YAML 里看起来是数字的值，有时候实际是字符串。

比如：

```text
接口返回 age = 18
YAML 里传期望结果 "18"
如果不转换，Python 会认为 18 != "18"
```

课程里的处理方式：

```text
断言类型是数字：期望结果转 float
断言类型是文本：期望结果转 str
```

这样能减少类型不一致导致的假失败。

### 第五步：真正比较

```python
if not comparators[operators](kwargs["实际结果"], kwargs["期望结果"]):
    ...
    raise Exception(...)
```

`comparators[operators]` 拿到一个函数。

比如 `operators` 是 `==`，就拿到：

```python
lambda a, b: a == b
```

然后：

```python
comparators[operators](实际结果, 期望结果)
```

就是：

```python
实际结果 == 期望结果
```

比较成功时，`if not True` 不成立，不抛异常，用例通过。  
比较失败时，`if not False` 成立，抛异常，pytest 判定用例失败。

---

## 第 2 块：断言包装方法

源码里还写了很多简单方法：

```python
def 断言文本相等(self, **kwargs):
    kwargs.update({"比较符": "=="})
    self.断言文本(**kwargs)

def 断言文本不相等(self, **kwargs):
    kwargs.update({"比较符": "!="})
    self.断言文本(**kwargs)

def 断言文本包含(self, **kwargs):
    kwargs.update({"比较符": "in"})
    self.断言文本(**kwargs)

def 断言数字相等(self, **kwargs):
    kwargs.update({"比较符": "==", "断言类型": "数字"})
    self.断言文本(**kwargs)

def 断言数字大于等于(self, **kwargs):
    kwargs.update({"比较符": ">=", "断言类型": "数字"})
    self.断言文本(**kwargs)

def 断言数字小于等于(self, **kwargs):
    kwargs.update({"比较符": "<=", "断言类型": "数字"})
    self.断言文本(**kwargs)

def 断言数字不等于(self, **kwargs):
    kwargs.update({"比较符": "!=", "断言类型": "数字"})
    self.断言文本(**kwargs)

def 断言数字小于(self, **kwargs):
    kwargs.update({"比较符": "<", "断言类型": "数字"})
    self.断言文本(**kwargs)

def 断言数字大于(self, **kwargs):
    kwargs.update({"比较符": ">", "断言类型": "数字"})
    self.断言文本(**kwargs)
```

这些方法都很短，但作用很大。

它们是给 YAML 用的“业务名字”。

YAML 里写：

```yaml
操作类型: 断言文本相等
```

比：

```yaml
操作类型: 断言文本
比较符: ==
```

更容易理解。

这就是“关键字封装”的思想：

```text
断言文本 是引擎
断言文本相等 / 包含 / 大于 是入口
```

一个地方改逻辑，很多入口都受益。

---

## 第 3 块：main.py 的完整注释理解

源码：`9月1号/main.py`

```python
import os

import pytest
from allure_combine import combine_allure

pytest_ars = [
    '-v',
    '-s',
    '--capture=sys',
    '--clean-alluredir',
    '--alluredir=allure-results',
    './HAT/core/TestRunner.py'
]

pytest.main(pytest_ars)

os.system('allure generate -c -o allure-report')

combine_allure('./allure-report')
```

逐个看：

```text
-v：显示更详细的执行结果
-s：显示 print 内容
--capture=sys：捕获系统输出，方便 Allure 生成 stdout 附件
--clean-alluredir：先清空旧的 Allure 数据
--alluredir=allure-results：指定测试数据目录
./HAT/core/TestRunner.py：指定要执行的测试文件
```

然后：

```python
pytest.main(pytest_ars)
```

执行测试。

```python
os.system('allure generate -c -o allure-report')
```

把 `allure-results` 生成 `allure-report`。

```python
combine_allure('./allure-report')
```

合并报告，生成可以直接打开的 `complete.html`。

---

## 第 4 块：YAML 里的断言怎么用

课程 YAML 中常见写法是：

```yaml
- 断言:
    操作类型: 断言文本相等
    期望结果: 登录成功
    实际结果: '{{my_msg}}'
```

流程是：

```text
1. 前一个步骤提取 msg
2. msg 保存成 my_msg
3. 渲染 {{my_msg}}
4. 断言文本相等调用断言文本
5. 比较实际结果和期望结果
```

---

## 断言解决什么问题

没有断言时：

```text
接口失败，程序可能不知道
接口返回错误信息，报告也可能显示通过
```

有断言后：

```text
实际结果 == 期望结果，通过
实际结果 != 期望结果，失败
报告能明确记录失败原因
```

自动化测试的价值，很大一部分来自断言。

---

## 适合什么场景

- 判断登录提示语。
- 判断接口返回的 code。
- 判断商品数量、库存、价格。
- 判断列表是否包含某个值。
- 判断删除后查询不到数据。

---

## 课后自测

1. `comparators` 字典里为什么用 `lambda`？
2. 断言文本和断言数字在类型处理上有什么区别？
3. `断言文本相等` 为什么要调用 `断言文本`？
4. 如果“实际结果”是数字 200，“期望结果”是字符串 `"200"`，课程代码会怎么处理？
5. 为什么 `raise Exception()` 会让 pytest 认为用例失败？

---
# 第10节：9月2号，DeepDiff 深度断言和批量断言

## 今天解决什么问题

昨天学的“断言文本”适合比较一个值。

但接口经常返回一大坨 JSON：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "user": {
      "id": 1001,
      "name": "alice",
      "email": "alice@example.com",
      "age": 25
    }
  }
}
```

如果你一个字段一个字段断言，会很累。

今天要学的是：

```text
把两个复杂 JSON 做深度对比
自动找出哪里不同
还能过滤动态字段
```

这就是 DeepDiff。

---

## 第 1 块：最简单的 DeepDiff

源码：`9月2号/examples_test/deepdiff1_test.py`

```python
from deepdiff import DeepDiff

exmsg = {"name": "alice", "age": 25}
sjmsg = {"name": "Alice", "age": 25}
diff = DeepDiff(exmsg, sjmsg)
print(diff)
assert not diff, f"不一致,{diff.pretty()}"
```

### DeepDiff 是什么

`DeepDiff` 可以比较两个 Python 对象，包括：

```text
字典
列表
嵌套字典
嵌套列表
```

它不只是比较两个对象是不是相等，还会告诉你“哪里不一样”。

比如上面的例子，它会提示 `name` 字段的值从 `"alice"` 变成了 `"Alice"`。

### `assert not diff`

DeepDiff 有个特点：

```text
没有差异：diff 是空字典 {}
有差异：diff 里有差异描述
```

所以：

```python
assert not diff
```

意思是：

```text
diff 为空，通过
diff 不为空，失败
```

`not {}` 是 `True`，所以没有差异时通过。  
`not {"values_changed": ...}` 是 `False`，所以有差异时失败。

### `diff.pretty()`

`diff.pretty()` 会把差异输出成人更容易看懂的文字。

比直接打印整个 diff 对象更直观。

---

## 第 2 块：复杂 JSON 对比

源码：`9月2号/examples_test/deepdiff2_test.py`

课程里准备了两个复杂字典：

### 实际结果

```python
actual_response = {
    "code": 200,
    "message": "SUCCESS",
    "data": {
        "user": {
            "id": 1001,
            "name": "alice",
            "email": "ALICE@EXAMPLE.COM",
            "age": 25,
            "hobbies": ["reading", "swimming", "coding"],
            "profile": {
                "level": "VIP",
                "score": 95.5,
                "tags": ["active", "new_user"]
            },
            "create_time": "2023-10-01 10:30:00"
        },
        "system_info": {
            "version": "1.2.3",
            "timestamp": 1696134600
        }
    }
}
```

### 预期结果

```python
expected_response = {
    "code": 200,
    "message": "success",
    "data": {
        "user": {
            "id": 1003,
            "name": "Alice",
            "email": "alice@example.com",
            "age": 25,
            "hobbies": ["coding", "reading", "swimming"],
            "profile": {
                "level": "vip",
                "score": 95.5,
                "tags": ["new_user", "active"]
            },
            "create_time": None
        },
        "system_info": {
            "version": "1.2.3",
            "timestamp": None
        }
    }
}
```

如果直接写：

```python
diff = DeepDiff(actual_response, expected_response)
assert not diff
```

用例会失败，因为确实有差异。

但其中有些差异“不该算失败”，比如：

```text
id 每次生成可能不同
create_time、timestamp 是动态时间
大小写可能是业务允许的
列表顺序变化不该影响判断
```

所以要用 DeepDiff 的参数来控制对比规则。

---

## 第 3 块：exclude_paths 过滤动态字段

课程代码：

```python
exclude_paths = [
    "root['data']['user']['id']",
    "root['data']['user']['create_time']",
    "root['data']['system_info']['timestamp']",
]
```

然后：

```python
diff = DeepDiff(actual_response, expected_response, exclude_paths=exclude_paths)
```

### `exclude_paths` 是什么

它是“排除路径”。

也就是告诉 DeepDiff：

```text
这些路径不用比。
```

路径写法是：

```text
root['层级1']['层级2']['字段名']
```

例如：

```text
root['data']['user']['id']
```

表示：

```text
根对象下的 data
data 下的 user
user 下的 id
```

这种写法适合过滤：

```text
动态 id
动态时间戳
随机 token
服务端生成的 create_time
```

---

## 第 4 块：忽略大小写和忽略顺序

课程最终判断：

```python
diff = DeepDiff(
    actual_response,
    expected_response,
    exclude_paths=exclude_paths,
    ignore_string_case=True,
    ignore_order=True
)
print(diff)
assert not diff, f"不一致,{diff.pretty()}"
print("测试通过")
```

### `ignore_string_case=True`

```text
alice 和 Alice 算一样
SUCCESS 和 success 算一样
```

适合接口文档对大小写不敏感，或者业务允许大小写不同的情况。

### `ignore_order=True`

```text
["reading", "swimming", "coding"]
["coding", "reading", "swimming"]
```

算一样。

适合列表只关心内容，不关心顺序的场景。

---

## 第 5 块：把 DeepDiff 封装成批量断言关键字

源码：`9月2号/HAT/keywords/api_keywords.py`

```python
def 批量断言(self, **kwargs):
    try:
        exmsg = kwargs["期望结果"]
        sjmsg = g_context().get_dict("响应结果").json()
        exclude_paths = kwargs.get("过滤字段", [])
        ignore_string_case = kwargs.get("忽略大小写", True)
        ignore_order = kwargs.get("忽略顺序", True)

        screen_data = {
            "exclude_paths": exclude_paths,
            "ignore_string_case": ignore_string_case,
            "ignore_order": ignore_order
        }
        diff = DeepDiff(sjmsg, exmsg, **screen_data)
    except Exception as e:
        assert False, f"批量断言失败,{e}"
    assert not diff, f"批量断言失败{diff.pretty()}"
```

### 这个方法做了什么

```text
1. 从 YAML 拿期望结果
2. 从全局变量拿实际响应
3. 拿过滤字段和忽略规则
4. 用 DeepDiff 对比
5. diff 不为空就断言失败
```

### `exmsg` 和 `sjmsg`

课程变量名可以理解为：

```text
exmsg：expected message，期望结果
sjmsg：shiji message，实际结果
```

实际结果来自：

```python
g_context().get_dict("响应结果").json()
```

所以它要求前面已经发过请求，并且请求关键字已经把 `response` 存进 `g_context`。

### `kwargs.get("过滤字段", [])`

YAML 里可以写：

```yaml
过滤字段:
  - "root['data']['user']['id']"
```

如果没写，默认给空列表。

### `kwargs.get("忽略大小写", True)`

课程里默认是 `True`。

也就是说，YAML 没写“忽略大小写”时，DeepDiff 会忽略大小写。

### `DeepDiff(sjmsg, exmsg, **screen_data)`

`**screen_data` 会把字典拆成关键字参数。

等价于：

```python
DeepDiff(
    sjmsg,
    exmsg,
    exclude_paths=exclude_paths,
    ignore_string_case=ignore_string_case,
    ignore_order=ignore_order
)
```

---

## 为什么有了普通断言还要批量断言

普通断言适合：

```text
只关心 msg
只关心 code
只关心某个字段
```

批量断言适合：

```text
完整校验响应结构
完整校验多个字段
回归测试接口返回内容没有变化
```

比如商品详情接口，你可能要同时确认：

```text
商品名
价格
状态
分类
图片地址
```

用批量断言就方便很多。

---

## 使用批量断言时要想清楚三件事

1. 哪些字段是动态的？要过滤。
2. 哪些字段大小写可以忽略？
3. 哪些列表顺序不重要？

不能无脑全忽略，否则可能漏掉真正的缺陷。

---

## 适合什么场景

- 响应体大、字段多。
- 回归测试时要求响应结构稳定。
- 对比接口重构前后的返回内容。
- 过滤动态字段后完整校验业务字段。

---

## 课后自测

1. `DeepDiff` 有差异时返回什么？没有差异时返回什么？
2. `exclude_paths` 的路径语法为什么要以 `root` 开头？
3. 什么时候适合开启 `ignore_order=True`？
4. 批量断言里为什么用 `g_context().get_dict("响应结果")`？
5. 如果把所有动态字段都加进 `exclude_paths`，会有什么风险？

---

# 第11节：9月3号，连接 MySQL 数据库和数据库断言

## 今天解决什么问题

接口测试不能只看接口返回。

很多关键结果要看数据库：

```text
注册接口，数据库有没有写入用户？
下单接口，数据库有没有生成订单？
删除接口，数据库记录有没有被删除？
```

所以今天要学：

```text
Python 连接 MySQL
执行 SQL
拿到查询结果
把结果放进全局变量
给后面的接口或断言用
```

---

## 第 1 块：先会用代码连接数据库

源码：`9月3号/examples_test/mysql_test.py`

课程把操作数据库分成 7 步：

```text
1. 连接数据库
2. 创建游标
3. 写 SQL
4. 执行 SQL
5. 获取结果
6. 关闭游标
7. 关闭数据库连接
```

### 完整示例结构

课程源码里有真实连接信息，这里用占位符代替敏感值：

```python
import pymysql
from pymysql import cursors

connect = pymysql.connect(
    host="<数据库服务器>",
    port=3306,
    user="<数据库用户名>",
    password="<数据库密码>",
    db="<数据库名>",
    cursorclass=cursors.DictCursor,
    charset='utf8'
)

cursor = connect.cursor()

sql = "select id, username from sxo_user where username='<测试账号>'"

cursor.execute(sql)

rs = cursor.fetchall()
print(rs)

cursor.close()
connect.close()
```

### `pymysql.connect(...)` 参数含义

```text
host：数据库服务器地址
port：数据库端口，MySQL 默认 3306
user：用户名
password：密码
db：要使用的数据库名
cursorclass：游标返回数据的形式
charset：字符集
```

### `cursorclass=cursors.DictCursor`

默认游标返回的数据像元组：

```python
[(2, 'youyi')]
```

`DictCursor` 返回字典：

```python
[{'id': 2, 'username': 'youyi'}]
```

字典更直观，也方便后面按字段名取值。

### 游标是什么

可以把游标理解成“执行 SQL 的小助手”。

```text
connect：负责和数据库保持连接
cursor：负责发 SQL、拿结果
```

用完要关：

```python
cursor.close()
connect.close()
```

不然连接会一直占用资源。

---

## 第 2 块：数据库配置放进 context.yaml

源文件：`9月3号/examples/api-cases-yaml/context.yaml`

课程源码里有真实数据库配置，这里按敏感信息处理：

```yaml
URL: http://shop-xo.hctestedu.com

_数据库:
  电商数据库:
    host: <数据库服务器>
    port: 3306
    user: <数据库用户名>
    password: <数据库密码>
    db: <电商数据库名>

  读书屋数据库:
    host: <数据库服务器>
    port: 3306
    user: <数据库用户名>
    password: <数据库密码>
    db: <读书屋数据库名>
```

### 为什么配置前面加 `_数据库`

因为 `context.yaml` 里既有普通业务变量，也有配置块。

加下划线可以把它当成“内部配置”，和普通变量区分开。

### 为什么数据库配置不能写死在关键字里

如果写死：

```text
换环境，要改代码。
换数据库，要改代码。
代码里有密码，也不安全。
```

放进 `context.yaml` 后：

```text
代码只负责逻辑
配置负责环境
敏感信息按公司规范管理
```

---

## 第 3 块：提取数据库MYSQL 关键字

源码：`9月3号/HAT/keywords/api_keywords.py`

```python
def 提取数据库MYSQL(self, **kwargs):
    results = {}
    db_config = g_context().get_dict("_数据库")[kwargs["数据库"]]
    config = {"cursorclass": cursors.DictCursor}
    config.update(db_config)

    connect = pymysql.connect(**config)
    cursor = connect.cursor()
    sql = kwargs["SQL"]
    cursor.execute(sql)
    rs = cursor.fetchall()
    print(rs)

    cursor.close()
    connect.close()

    var_names = kwargs.get("变量名", [])

    if not var_names:
        for i, item in enumerate(rs, start=1):
            for key, value in item.items():
                results[f"{key}_{i}"] = value
    else:
        field = len(rs[0]) if rs else 0
        if len(var_names) != field:
            raise Exception(f"变量名数量和结果数量不一致{var_names}")

        for idx, item in enumerate(rs, start=1):
            for col_idx, key in enumerate(item):
                results[f"{var_names[col_idx]}_{idx}"] = item[key]

    g_context().set_by_dict(results)
    print("数据库结果保存在变量中", g_context().show_dict())
```

### 第一步：找数据库配置

```python
db_config = g_context().get_dict("_数据库")[kwargs["数据库"]]
```

YAML 里会写：

```yaml
数据库: 电商数据库
```

程序就取：

```python
g_context["_数据库"]["电商数据库"]
```

也就是：

```python
{
    "host": "...",
    "port": 3306,
    ...
}
```

### 第二步：统一配置

```python
config = {"cursorclass": cursors.DictCursor}
config.update(db_config)
```

先写死游标类型是 `DictCursor`。

再 `update(db_config)`，把 YAML 里的数据库配置合并进去。

最后：

```python
pymysql.connect(**config)
```

把字典拆成关键字参数传给 `pymysql.connect()`。

### 第三步：执行 SQL

```python
sql = kwargs["SQL"]
cursor.execute(sql)
rs = cursor.fetchall()
```

YAML 里可能写：

```yaml
SQL: "select id,username from sxo_user where username='<测试账号>'"
```

`cursor.execute(sql)` 执行 SQL。  
`cursor.fetchall()` 拿所有结果。

### 第四步：保存变量

如果查询结果是：

```python
[
    {'id': 2, 'username': 'youyi'},
    {'id': 3, 'username': 'youer'}
]
```

YAML 里写：

```yaml
变量名: [uid, uname]
```

程序会生成：

```python
{
    'uid_1': 2,
    'uname_1': 'youyi',
    'uid_2': 3,
    'uname_2': 'youer'
}
```

为什么变量名后面要加 `_1`、`_2`？

因为可能查出多行数据。

```text
uid_1：第 1 行的 id
uid_2：第 2 行的 id
```

这样多行结果不会互相覆盖。

### 如果没有写变量名

YAML 里这样：

```yaml
变量名: []
```

或者不写变量名，程序会用数据库字段名生成变量：

```python
{
    'id_1': 2,
    'username_1': 'youyi',
    'id_2': 3,
    'username_2': 'youer'
}
```

### 校验变量名数量

```python
field = len(rs[0]) if rs else 0
if len(var_names) != field:
    raise Exception(f"变量名数量和结果数量不一致{var_names}")
```

比如查询结果是两列：

```python
{'id': 2, 'username': 'youyi'}
```

那 `var_names` 就必须有两个名字。

```yaml
变量名: [uid, uname]
```

如果只写一个，就报错。

---

## 第 4 块：YAML 里怎么用

课程源码里出现在 `9月3号/examples/api-cases-yaml/1_login.yaml`，结构类似：

```yaml
- 提取数据库数据:
    操作类型: 提取数据库MYSQL
    数据库: 电商数据库
    SQL: "select id,username from sxo_user where username='<测试账号>'"
    变量名: [uid, uname]

- 断言:
    操作类型: 断言文本相等
    期望结果: "<测试账号>"
    实际结果: "{{username_1}}"
```

流程：

```text
1. 连接电商数据库
2. 查询用户
3. username_1 保存数据库里的用户名
4. 渲染 {{username_1}}
5. 用断言文本相等判断
```

---

## 为什么测试要查数据库

接口返回成功，不一定代表业务正确。

比如：

```text
接口返回“注册成功”
但数据库没有用户记录
这就是严重缺陷
```

数据库校验适合：

```text
新增数据
修改数据
删除数据
状态流转
金额计算
库存扣减
```

---

## 安全提醒

数据库账号、密码、服务器地址都是敏感信息。

课程资料里有真实值，是为了让你能直接上课练习。  
你自己写笔记、发报告、传代码时，不要到处粘贴真实密码。  
企业里通常用配置中心、环境变量、加密配置或独立测试账号来管理。

---

## 课后自测

1. `DictCursor` 和默认游标返回的数据有什么区别？
2. 为什么查出来的变量名要加 `_1`、`_2`？
3. `pymysql.connect(**config)` 里的 `**config` 是什么意思？
4. 为什么数据库配置要放在 `context.yaml`？
5. 哪些接口测试场景必须查数据库？举三个例子。

---

# 第12节：9月4号，数据驱动测试 DDT

## 今天解决什么问题

登录接口通常要测很多组数据：

```text
正确账号 + 正确密码 -> 登录成功
错误账号 + 正确密码 -> 账号不存在
正确账号 + 错误密码 -> 密码错误
账号为空 -> 参数错误
密码为空 -> 参数错误
```

如果每组数据都复制一个 YAML 文件，会非常麻烦。

今天要做的是：

```text
一套用例步骤 + 多组测试数据 = 多条用例
```

这就是数据驱动，英文常说 Data Driven Testing，缩写 DDT。

---

## 先看用例模板

源文件：`9月4号/examples/api-cases-yaml/0_loginddt.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 商城登录用例标题
  一级模块: 登录模块
  二级模块: 登录功能

前置脚本: ""
后置脚本: ""

用例步骤:
- 发送登陆接口:
    操作类型: 发送请求POST
    请求地址: '{{URL}}'
    URL参数:
      s: /api/user/login
      application: app
      application_client_type: weixin
    请求数据:
      accounts: "{{uname}}"
      pwd: "{{pwd}}"
      type: "username"

- 提取响应数据msg:
    操作类型: 提取数据JSON
    表达式: $..msg
    下标: 0
    变量名: my_msg

- 断言:
    操作类型: 断言文本相等
    期望结果: "{{expected}}"
    实际结果: "{{my_msg}}"
```

注意这里用了三个变量：

```text
{{uname}}：账号
{{pwd}}：密码
{{expected}}：期望结果
```

它们不来自 `context.yaml`，而来自后面的“数据驱动”。

---

## 数据驱动部分

同一个文件下面：

```yaml
数据驱动:
  - 描述标题: 正确用户名和密码
    uname: <正确账号>
    pwd: <正确密码>
    expected: 登录成功

  - 描述标题: 错误用户名和正确密码
    uname: <不存在账号>
    pwd: <正确密码>
    expected: "登录帐号不存在"

  - 描述标题: 正常用户名和错误密码
    uname: <正确账号>
    pwd: <错误密码>
    expected: "密码错误"
```

课程源码里有具体测试账号和密码，这里用占位符代替。

这份数据会被解析器拆成三条用例。

---

## 第 1 块：yaml_case_parser

源码：`9月4号/HAT/parse/YamlCaseParser.py`

```python
def yaml_case_parser(file_path):
    case_names = []
    case_infos = []
    yaml_caseInfos = load_yaml_files(file_path)

    for caseinfo in yaml_caseInfos:
        ddts = caseinfo.get("数据驱动", [])

        if len(ddts) == 0:
            case_name = caseinfo.get("基础配置").get("用例标题", uuid.uuid4().__str__())
            case_names.append(case_name)
            case_infos.append(caseinfo)
        else:
            caseinfo.pop("数据驱动")
            for ddt in ddts:
                new_case = copy.deepcopy(caseinfo)
                new_case.update({"local_context": ddt})
                case_name = caseinfo.get("基础配置").get("用例标题", uuid.uuid4().__str__())
                case_name = f'{case_name}-{ddt.get("描述标题", uuid.uuid4().__str__())}'
                new_case.get("基础配置").update({"用例标题": case_name})
                case_names.append(case_name)
                case_infos.append(new_case)

    return {
        "case_infos": case_infos,
        "case_names": case_names
    }
```

### 先理解返回值

```python
return {
    "case_infos": case_infos,
    "case_names": case_names
}
```

它返回两样东西：

```text
case_names：所有用例标题
case_infos：所有用例数据
```

后面 pytest 参数化会用到它们。

### 第一步：加载所有 YAML

```python
yaml_caseInfos = load_yaml_files(file_path)
```

它会扫描文件夹里的编号 YAML 文件。

### 第二步：找“数据驱动”

```python
ddts = caseinfo.get("数据驱动", [])
```

如果 YAML 里没有 `数据驱动`，`ddts` 是空列表。

### 第三步：没有数据驱动的处理

```python
if len(ddts) == 0:
    case_name = caseinfo.get("基础配置").get("用例标题", uuid.uuid4().__str__())
    case_names.append(case_name)
    case_infos.append(caseinfo)
```

也就是：

```text
一条 YAML 就是一条用例。
```

如果标题没写，就用 UUID 生成一个，保证每条用例都有名字。

### 第四步：有数据驱动的处理

先删掉原始的 `数据驱动` 字段：

```python
caseinfo.pop("数据驱动")
```

为什么要 pop？

因为后面生成的每条用例应该是“用例模板”，不应该再把整套数据驱动数据带进去。

然后循环每组数据：

```python
for ddt in ddts:
    ...
```

每组数据生成一条新用例。

### 第五步：深拷贝用例模板

```python
new_case = copy.deepcopy(caseinfo)
```

为什么必须深拷贝？

因为每条新用例要独立。

比如用例模板里有：

```python
"用例步骤": [...]
```

如果只是浅拷贝，多个 `new_case` 可能还指向同一份内部数据。

当第一条用例更新标题或 local_context 时，可能影响第二条。

深拷贝就是给每条数据驱动用例一份独立副本。

### 第六步：把每组数据放进 local_context

```python
new_case.update({"local_context": ddt})
```

比如第一组数据：

```python
{
    "描述标题": "正确用户名和密码",
    "uname": "...",
    "pwd": "...",
    "expected": "登录成功"
}
```

会被保存到：

```python
new_case["local_context"]
```

后面执行器渲染变量时会把它合并进 context。

### 第七步：拼新标题

```python
case_name = caseinfo.get("基础配置").get("用例标题", uuid.uuid4().__str__())
case_name = f'{case_name}-{ddt.get("描述标题", uuid.uuid4().__str__())}'
```

原始标题是：

```text
商城登录用例标题
```

每组数据有“描述标题”，最后标题会变成类似：

```text
商城登录用例标题-正确用户名和密码
商城登录用例标题-错误用户名和正确密码
商城登录用例标题-正常用户名和错误密码
```

这样报告里一眼能看出每条用例测的是什么。

---

## 第 2 块：执行器怎么用解析结果

源码：`9月4号/HAT/core/TestRunner.py`

```python
alldata = yaml_case_parser(r'./examples/api-cases-yaml/')
data = alldata["case_infos"]

@pytest.mark.parametrize('caseinfo', data)
def test_case_exceute(self, caseinfo):
    ...
```

这里的关键：

```text
yaml_case_parser() 返回字典
data 取其中的 case_infos
pytest.mark.parametrize 会把列表拆成多条用例
```

如果 `data` 里有 3 条用例，pytest 就会执行 3 次。

---

## 第 3 块：执行器里合并 local_context

在测试方法里：

```python
local_context = caseinfo.get("local_context", {})
context = copy.deepcopy(g_context().show_dict())
context.update(local_context)
```

这部分用于前置脚本。

在步骤循环里：

```python
context = copy.deepcopy(g_context().show_dict())
context.update(local_context)
step_value = eval(refresh(step_value, context))
```

这部分用于用例步骤。

也就是说，每组 DDT 数据会变成“本条用例自己的变量”。

---

## 一条 DDT 用例的实际运行过程

以“正确用户名和密码”为例：

```text
1. 解析器生成一条 new_case
2. local_context 里有 uname、pwd、expected
3. 执行器取 g_context 全局变量
4. 合并 local_context
5. 渲染 {{URL}}
6. 渲染 {{uname}}、{{pwd}}
7. 发送登录请求
8. 提取 msg，保存成 my_msg
9. 渲染 {{expected}} 和 {{my_msg}}
10. 断言成功或失败
```

另外两组数据重复同样流程，只是变量不同。

---

## DDT 的价值

没有 DDT：

```text
一个登录场景写一个文件
维护成本高
YAML 里重复内容多
```

有 DDT：

```text
步骤只写一遍
数据清晰集中
新增场景只要加一组数据
报告里每条数据一个标题
```

这和自动化里的“用例与数据分离”思想一致。

---

## 适合什么场景

- 登录、注册、表单校验。
- 参数边界测试：为空、超长、特殊字符。
- 不同角色权限测试。
- 同一个接口不同输入组合。
- 批量新增、批量校验。

---

## 课后自测

1. `数据驱动` 列表里有 5 组数据，最终会生成几条用例？
2. 为什么 `yaml_case_parser()` 里要用 `copy.deepcopy()`？
3. `local_context` 是怎么进入变量渲染过程的？
4. 为什么每条 DDT 用例的标题要拼上“描述标题”？
5. 如果某组数据缺少 `expected`，执行到断言时会发生什么？

---

# 第13节：9月5号，loguru 日志和步骤日志收集

## 今天解决什么问题

接口测试失败时，只有一句“断言失败”往往不够。

你还要知道：

```text
发请求前参数是什么？
渲染后参数是什么？
有没有提取到 token？
异常发生在哪一步？
```

所以今天加两样东西：

```text
loguru：记录日志
allure_step_logger：把每个步骤的日志放进 Allure 报告
```

---

## 第 1 块：先认识 loguru 日志级别

源码：`9月5号/examples_test/test_log.py`

```python
from loguru import logger

logger.debug("调试级别")
logger.info("程序正常运行级别")
logger.warning("警告级别")
logger.error("错误信息级别")
```

常见级别从低到高：

```text
DEBUG：调试细节，开发测试时看
INFO：正常流程信息
WARNING：有潜在问题，但不影响继续运行
ERROR：出错了
```

课程里还有保存文件：

```python
logger.add(
    "log_{time}.log",
    rotation="100MB",
    retention="10days"
)
```

含义：

```text
log_{time}.log：按时间生成日志文件
rotation="100MB"：日志到 100MB 就轮转
retention="10days"：日志保留 10 天
```

---

## 第 2 块：main.py 里的全局日志配置

源码：`9月5号/main.py`

```python
import os
import sys
import time

import pytest
from allure_combine import combine_allure
from loguru import logger

if not os.path.exists('./HAT/logs'):
    os.mkdir('./HAT/logs')

time_str = time.strftime('%Y-%m-%d %H-%M-%S', time.localtime())
log_level = os.getenv("HAT_LOG_LEVEL", "DEBUG").upper()

logger.configure(
    handlers=[
        {"sink": sys.stdout, "level": "INFO"},
        {"sink": os.path.join("./HAT/logs", f"hat_{time_str}.log"), "level": log_level}
    ]
)
```

### 先创建 logs 目录

```python
if not os.path.exists('./HAT/logs'):
    os.mkdir('./HAT/logs')
```

目录不存在就创建。

这样可以避免写日志时报“目录不存在”。

### 生成时间字符串

```python
time_str = time.strftime('%Y-%m-%d %H-%M-%S', time.localtime())
```

比如生成：

```text
2026-09-06 19-30-25
```

为什么要放进文件名？

因为每次执行测试都生成独立日志文件，方便按时间排查。

### 从环境变量读日志级别

```python
log_level = os.getenv("HAT_LOG_LEVEL", "DEBUG").upper()
```

`os.getenv()` 用来读环境变量。

如果没有设置 `HAT_LOG_LEVEL`，默认 `DEBUG`。

`.upper()` 是转大写，比如用户写 `debug`，会变成 `DEBUG`。

### 两个日志输出位置

```python
logger.configure(
    handlers=[
        {"sink": sys.stdout, "level": "INFO"},
        {"sink": ".../hat_时间.log", "level": log_level}
    ]
)
```

意思是：

```text
控制台：显示 INFO 及以上
文件：按 HAT_LOG_LEVEL 显示，默认 DEBUG 及以上
```

所以文件日志通常比控制台更详细。

---

## 第 3 块：StepLogCollector

源码：`9月5号/HAT/utils/allure_step_logger.py`

```python
import io
from contextlib import contextmanager

import allure
from loguru import logger


class StepLogCollector:
    def __init__(self):
        self.log_buffer = io.StringIO()
        self.sink_id = None

    def __enter__(self):
        self.sink_id = logger.add(self.log_buffer, level="DEBUG")
        return self

    def __exit__(self, exc_type, exc_val, exc_tb):
        logger.remove(self.sink_id)
        log_content = self.log_buffer.getvalue()
        if log_content.strip():
            allure.attach(
                log_content,
                name="步骤日志",
                attachment_type=allure.attachment_type.TEXT
            )
        self.log_buffer.close()
```

### 它要解决的问题

每个 Allure 步骤都希望有自己的日志：

```text
步骤开始 -> 收集这个步骤里的日志 -> 步骤结束 -> 附件挂到报告
```

不能所有日志混在一起，否则很难定位是哪一步出的问题。

### `io.StringIO()`

```python
self.log_buffer = io.StringIO()
```

它像一块“内存里的笔记本”。

日志不先写文件，而是先写进这块内存。

### `__enter__` 和 `__exit__`

这两个魔法方法让类支持 `with` 语法。

```python
with StepLogCollector() as collector:
    ...
```

执行流程：

```text
进入 with：自动调用 __enter__
执行代码块
离开 with：自动调用 __exit__
```

### `__enter__` 里做了什么

```python
self.sink_id = logger.add(self.log_buffer, level="DEBUG")
return self
```

给 loguru 添加一个新的日志输出位置。

这个位置就是 `self.log_buffer`。

`logger.add()` 会返回一个 sink id，方便以后移除。

### `__exit__` 里做了什么

```python
logger.remove(self.sink_id)
```

先移除这个日志收集器，避免一直收集。

```python
log_content = self.log_buffer.getvalue()
```

读取内存日志内容。

```python
if log_content.strip():
    allure.attach(...)
```

如果日志不是空的，就作为附件挂到 Allure。

```python
self.log_buffer.close()
```

最后关闭内存缓冲区。

---

## 第 4 块：allure_step_with_log

同一个源码文件下面：

```python
@contextmanager
def allure_step_with_log(step_name):
    with allure.step(step_name):
        with StepLogCollector() as collector:
            yield collector
```

### `@contextmanager`

这是 Python 的上下文管理器装饰器。

它让一个函数也能支持 `with`。

### 这段代码的执行顺序

```python
with allure_step_with_log('发送登陆接口'):
    # 这里执行步骤代码
```

流程是：

```text
1. 进入 allure.step(step_name)
2. 进入 StepLogCollector，开始收集日志
3. yield collector，暂停，去执行 with 里面的代码
4. with 里面的代码执行完
5. 离开 StepLogCollector，把日志挂到 Allure
6. 离开 allure.step
```

`yield` 可以理解成“中间停一下”。

它把 with 里面的执行时机切出来。

---

## 第 5 块：执行器里的使用

源码：`9月5号/HAT/core/TestRunner.py`

原来步骤循环里是：

```python
with allure.step(step_name):
    ...
```

现在改成：

```python
with allure_step_with_log(step_name):
    ...
```

变化不大，但效果变强了：

```text
原来：只有 Allure 步骤
现在：Allure 步骤 + 本步骤日志附件
```

---

## 为什么日志对测试框架很重要

日志是排查问题的证据。

比如一个用例失败：

```text
断言失败：登录成功 != 密码错误
```

如果只有这一句，你不知道账号传成了什么。

有日志后能看到：

```text
渲染前：accounts: '{{uname}}'
渲染后：accounts: 'xxx'
响应：msg=密码错误
```

这样能快速判断是数据问题、环境问题，还是代码问题。

---

## 适合什么场景

- 接口链路长，失败后需要定位步骤。
- 测试报告要给开发或领导看。
- 出现偶发失败，需要保留现场。
- 需要把请求参数、响应结果留档。

---

## 课后自测

1. DEBUG 和 INFO 日志级别有什么区别？
2. `logger.add()` 为什么会返回 sink id？
3. `StepLogCollector` 的 `__enter__` 和 `__exit__` 分别做什么？
4. `yield` 在 `allure_step_with_log` 里起什么作用？
5. 为什么日志附件要判断 `log_content.strip()` 非空？

---
# 第14节：9月6号，Session 复用和动态扩展关键字

## 今天解决什么问题

前面执行器里每次都写：

```python
keywords = Keywords(requests)
```

但真实项目里有两种情况：

```text
1. 每次请求都用全新会话
2. 整个用例或整个流程复用同一个 Session
```

有些系统依赖 Cookie、登录状态、服务端 Session，这时就不能随便新建会话。

所以今天要解决：

```text
根据配置决定：用 requests 还是 requests.session()
并且全局复用同一个 session 对象
```

同时，执行器里动态关键字的查找逻辑也被整理成了 `ex_invoke()`。

---

## 第 1 块：context.yaml 增加 session_reuse

源文件：`9月6号/examples/api-cases-yaml/context.yaml`

在原来的全局配置后面，课程加入了：

```yaml
session_reuse: True
key_dir: ./HAT/key_dir
```

### `session_reuse`

意思是：

```text
True：复用 Session
False 或不配置：不复用
```

### `key_dir`

指定扩展关键字目录。

后面动态关键字找不到时，会去这个目录找同名 Python 文件。

---

## 第 2 块：ApiCaseContext

源码：`9月6号/HAT/context/ApiCaseContext.py`

```python
import requests

from HAT.core.globalContext import g_context
from HAT.keywords.api_keywords import Keywords


_global_request_obj = None


class ApiCaseContext:
    def __init__(self):
        self.request = None
        self.keywords = None

    def init_keywords(self):
        session_reuse = g_context().get_dict("session_reuse")

        if session_reuse is not None and session_reuse == True:
            global _global_request_obj
            if _global_request_obj is None:
                _global_request_obj = requests.session()
            self.request = _global_request_obj
        else:
            self.request = requests.session()

        self.keywords = Keywords(self.request)
        return self.keywords
```

### 先看模块级全局变量

```python
_global_request_obj = None
```

它写在文件里、类外面，所以是模块级全局变量。

它的作用是保存唯一的 session 对象。

### `init_keywords()` 的判断逻辑

```python
session_reuse = g_context().get_dict("session_reuse")
```

从全局变量里拿配置。

```python
if session_reuse is not None and session_reuse == True:
```

这里有两个条件：

```text
session_reuse 不是 None
session_reuse 是 True
```

只有两个条件都满足，才进入复用逻辑。

### 复用 Session 的代码

```python
global _global_request_obj

if _global_request_obj is None:
    _global_request_obj = requests.session()

self.request = _global_request_obj
```

这里要特别注意 `global _global_request_obj`。

在函数里如果要修改模块级全局变量，需要先写 `global`。

流程是：

```text
第一次调用 init_keywords()：
_global_request_obj 是 None
创建 requests.session()
以后再用

后面再调用 init_keywords()：
_global_request_obj 已经有 session
不再创建
直接复用
```

这就是“全局复用同一个 session”。

### 不复用 Session

```python
self.request = requests.session()
```

每次调用 `init_keywords()` 都新建一个 session。

适合每次请求都要求独立会话的场景。

### 创建 Keywords

```python
self.keywords = Keywords(self.request)
return self.keywords
```

之前执行器写：

```python
Keywords(requests)
```

现在改成：

```python
Keywords(session对象)
```

关键字内部仍然是：

```python
self.request.request('post', **requests_data)
```

因为 `requests.session()` 对象也有 `.request()` 方法，所以关键字代码不用大改。

这就是前面封装 `self.request` 的好处。

---

## 第 3 块：执行器里的调用变化

源码：`9月6号/HAT/core/TestRunner.py`

原来：

```python
keywords = Keywords(requests)
```

现在：

```python
if base_info.get("用例类型") == 'ApiCase':
    keywords = ApiCaseContext().init_keywords()
```

也就是根据用例类型初始化关键字。

执行流程变成：

```text
1. 读取用例的基础配置
2. 如果用例类型是 ApiCase
3. 创建 ApiCaseContext
4. init_keywords() 根据 session_reuse 决定请求对象
5. 返回 Keywords
```

---

## Session 复用解决了什么业务问题

很多系统有登录状态。

比如：

```text
登录后，服务端返回 Cookie
后续请求要带上 Cookie
如果每次新建 session，Cookie 可能丢失
```

复用 Session 后：

```text
第一个请求拿到的 Cookie
会保留在同一个 session 里
后面的请求自动带上
```

它和 token 关联不一样：

```text
token 关联：手动提取 token，放到参数里
Session 复用：requests.session 自动维护 Cookie 等会话信息
```

---

## `requests` 和 `requests.session()` 的区别

```text
requests：普通模块，每次请求相对独立
requests.session()：会话对象，会自动维护 Cookie 等状态
```

课程关键字把请求对象抽成 `self.request`，就是为了两者都能用。

---

## 第 4 块：ex_invoke 动态扩展关键字

源码：`9月6号/HAT/keywords/api_keywords.py`

```python
def ex_invoke(self, **kwargs):
    key = kwargs['key']
    if g_context().get_dict("key_dir") is not None:
        sys.path.append(g_context().get_dict("key_dir"))
        module = __import__(key)
        class_ = getattr(module, key)
        key_func = class_(requests).__getattribute__(key)
        key_func(**kwargs['step_value'])
```

### 它什么时候被调用

执行器里步骤循环有：

```python
try:
    key_func = keywords.__getattribute__(key)
    key_func(**step_value)
except AttributeError:
    if g_context().get_dict("key_dir") is not None:
        keywords.ex_invoke(key=key, step_value=step_value)
except Exception as e:
    raise e
```

意思是：

```text
1. 先在 Keywords 类里找方法
2. 找不到时发生 AttributeError
3. 去全局变量里拿 key_dir
4. 调用 ex_invoke() 加载外部关键字
```

### `sys.path.append(key_dir)`

Python 导入模块时，会去 `sys.path` 里的路径找。

```python
sys.path.append(g_context().get_dict("key_dir"))
```

就是把扩展关键字目录加进去，这样后面才能导入目录下的 Python 文件。

### `__import__(key)`

如果 `key` 是：

```text
发送请求POST
```

就导入：

```text
发送请求POST.py
```

### `getattr(module, key)`

从导入的模块里找同名类。

例如：

```python
class 发送请求POST:
    ...
```

### `class_(requests).__getattribute__(key)`

这一串拆开看：

```python
obj = class_(requests)
key_func = obj.__getattribute__(key)
```

也就是：

```text
创建外部关键字类对象
再找同名方法
```

### 最后执行

```python
key_func(**kwargs['step_value'])
```

把 YAML 步骤数据传给外部关键字方法。

---

## 为什么要做动态扩展

如果所有关键字都只能写在 `api_keywords.py` 里，会有两个问题：

```text
文件越来越大
多人协作容易冲突
```

有了 `key_dir + ex_invoke`：

```text
同事可以自己加关键字文件
核心执行器不用频繁改
文件名、类名、方法名保持一致即可
```

---

## 适合什么场景

- 登录态依赖 Cookie。
- 多个接口要保持在同一个会话。
- 不同业务团队扩展自己的关键字。
- 核心框架保持稳定，扩展能力交给插件或 key_dir。

---

## 课后自测

1. `session_reuse: True` 时，session 对象什么时候创建？
2. `global _global_request_obj` 如果不写，函数里会遇到什么问题？
3. Session 复用和 token 提取有什么区别？
4. `ex_invoke()` 为什么要求文件名、类名、方法名一致？
5. 哪些场景不适合复用 Session？

---

# 第15节：9月7号，上传、下载和加密接口

## 今天解决什么问题

前面主要测普通 JSON 或表单接口。

真实项目还有三类常见接口：

```text
上传接口：传图片、附件
下载接口：拿文件回来
加密接口：参数要先加密再发送
```

今天专门处理这三类。

---

## 第 1 块：上传接口 YAML

源文件：`9月7号/examples/api-cases-yaml/upload.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 上传用例标题
  一级模块: 上传模块
  二级模块: 上传功能

前置脚本: ""
后置脚本: ""

用例步骤:
- 发送上传接口:
    操作类型: 发送请求POST
    请求地址: http://127.0.0.1:5000/upload
    请求数据:
      image: ./HAT/img/2.png
    请求类型: files
```

重点是：

```yaml
请求类型: files
```

之前请求类型主要是：

```text
json
data
```

现在多了一个：

```text
files
```

---

## 第 2 块：发送请求POST 支持文件上传

源码：`9月7号/HAT/keywords/api_keywords.py`

在 `发送请求POST` 里新增了 `files` 分支：

```python
elif data_type == "files":
    if isinstance(data, dict):
        files = {}
        for key, file_path in data.items():
            files[key] = open(file_path, "rb")
        requests_data["files"] = files
    else:
        file = open(data, "rb")
        requests_data["files"] = {"file": file}
else:
    raise Exception("请求类型错误，仅支持json/data")
```

### 为什么要判断 `isinstance(data, dict)`

因为课程 YAML 里的写法是：

```yaml
请求数据:
  image: ./HAT/img/2.png
```

读取后就是字典：

```python
{'image': './HAT/img/2.png'}
```

这时要构造：

```python
{'image': open('./HAT/img/2.png', 'rb')}
```

如果传入的不是字典，而是一个路径字符串，就用默认字段名 `file`：

```python
{'file': open(data, 'rb')}
```

### `open(file_path, 'rb')`

```text
r：read，读
b：binary，二进制
```

图片、PDF、压缩包等文件要用二进制方式读取。

### `requests_data["files"]`

`requests` 上传文件的写法通常是：

```python
requests.post(url, files={'image': open('xxx.png', 'rb')})
```

课程关键字把这种写法封装进 YAML。

---

## 第 3 块：Flask 上传服务

源码：`9月7号/examples_test/P01_4 flask应用/uploadFile.py`

```python
from flask import Flask, request, jsonify
import os

app = Flask(__name__)


@app.route('/upload', methods=['POST'])
def upload():
    if 'image' not in request.files:
        data = {
            'msg': '当前未填写image参数',
            'code': 400,
        }
        return jsonify(data)

    file = request.files['image']
    print(file.filename)

    save_path = os.path.join('img', file.filename)
    file.save(save_path)

    data = {
        'msg': 'ok',
        'code': 200
    }
    return jsonify(data)


if __name__ == '__main__':
    app.run(debug=True)
```

### 这段服务端代码在干嘛

```text
1. 定义 POST /upload 接口
2. 检查请求里有没有 image 文件字段
3. 没有 image 就返回 400
4. 有 image 就取文件
5. 拼保存路径
6. 保存文件
7. 返回成功 JSON
```

### `request.files`

Flask 里：

```text
request.form：普通表单字段
request.files：上传的文件
```

因为客户端传的是文件，所以服务端用 `request.files['image']`。

### `file.save(save_path)`

把上传的文件保存到服务器指定路径。

---

## 第 4 块：下载接口 YAML

源文件：`9月7号/examples/api-cases-yaml/download.yaml`

```yaml
基础配置:
  用例类型: ApiCase
  用例标题: 下载用例标题
  一级模块: 下载模块
  二级模块: 下载功能

前置脚本: ""
后置脚本: ""

用例步骤:
- 发送上传接口:
    操作类型: 下载接口get
    请求地址: http://127.0.0.1:5001/download/login1.csv
    保存路径: ./HAT/img/login1.csv
```

注意：

```text
操作类型是 下载接口get
不是 发送请求GET
```

因为下载要额外处理“保存路径”。

---

## 第 5 块：下载接口关键字

源码：`9月7号/HAT/keywords/api_keywords.py`

```python
def 下载接口get(self, **kwargs):
    url = kwargs.get('请求地址', None)
    params = kwargs.get('URL参数', None)
    headers = kwargs.get('请求头', None)
    data = kwargs.get('请求数据', None)
    files = kwargs.get('文件列表', [])
    save_path = kwargs.get("保存路径")
    stream = kwargs.get("流式下载", False)
    chunk_size = kwargs.get("块大小", 1024)

    request_data = {
        "url": url,
        "params": params,
        "headers": headers,
        "files": files,
        "stream": stream if save_path else False
    }

    response = self.request.request("get", **request_data)
    if save_path:
        os.makedirs(os.path.dirname(save_path), exist_ok=True)
        if stream:
            with open(save_path, "wb") as f:
                for chunk in response.iter_content(chunk_size=chunk_size):
                    f.write(chunk)
        else:
            with open(save_path, "wb") as f:
                f.write(response.content)
    return response
```

### 先拿参数

重点参数有三个：

```text
保存路径：文件存到哪里
流式下载：是否分块下载
块大小：每次写多少字节
```

### `stream`

```python
"stream": stream if save_path else False
```

如果 YAML 没传保存路径，就不需要把响应保留成流。

如果有保存路径，才按 YAML 里的 `流式下载` 决定。

### 创建目录

```python
os.makedirs(os.path.dirname(save_path), exist_ok=True)
```

`os.path.dirname(save_path)` 取路径中的目录部分。

例如：

```text
./HAT/img/login1.csv
```

目录部分是：

```text
./HAT/img
```

`exist_ok=True` 表示目录已经存在也不报错。

### 普通下载

```python
with open(save_path, "wb") as f:
    f.write(response.content)
```

把响应体的二进制内容一次性写入文件。

适合小文件。

### 流式下载

```python
with open(save_path, "wb") as f:
    for chunk in response.iter_content(chunk_size=chunk_size):
        f.write(chunk)
```

每次只取一块内容，写一块。

适合大文件，避免一次性占用太多内存。

---

## 第 6 块：Flask 下载服务

源码：`9月7号/examples_test/P01_4 flask应用/download_file.py`

```python
from flask import Flask, send_file, abort
import os

app = Flask(__name__)


@app.route('/download/<filename>')
def download_file(filename):
    file_path = os.path.join("img", filename)

    if not os.path.exists(file_path):
        abort(404, description="File not found")

    return send_file(
        file_path,
        as_attachment=True,
        download_name=filename
    )


if __name__ == '__main__':
    app.run(debug=True, port=5001)
```

### 路由里的 `<filename>`

```python
@app.route('/download/<filename>')
```

这是一个动态路由。

访问：

```text
/download/login1.csv
```

时，`filename` 就是：

```text
login1.csv
```

### `send_file()`

它把文件作为响应返回给客户端。

```python
as_attachment=True
```

表示作为附件下载。

---

## 第 7 块：AES 加密

源码：`9月7号/examples_test/加密数据.py`

课程源码里有一个约定好的密钥，写笔记时不要到处传播。这里只讲结构：

```python
import base64

from Crypto.Cipher import AES
from Crypto.Util.Padding import pad, unpad

key = b'<课程约定的AES密钥>'
data = '<要加密的原始数据>'

data = data.encode('utf-8')

cipher = AES.new(key, AES.MODE_ECB)
ct_bytes = cipher.encrypt(pad(data, AES.block_size))
ct = base64.b64encode(ct_bytes).decode('utf-8')
print("加密数据", ct)
```

解密部分：

```python
ci_bytest = base64.b64decode(ct)
cipher = AES.new(key, AES.MODE_ECB)
pt = unpad(cipher.decrypt(ci_bytest), AES.block_size)
c = pt.decode("utf-8")
print("解密数据", c)
```

### 为什么先 `encode('utf-8')`

AES 处理的是字节数据，不是普通字符串。

```text
字符串：'123456'
字节：b'123456'
```

所以要：

```python
data = data.encode('utf-8')
```

### `AES.new(key, AES.MODE_ECB)`

创建 AES 加解密对象。

```text
key：密钥
AES.MODE_ECB：一种分组加密模式
```

课堂重点是理解流程，不要把所有项目都套 ECB。实际项目用哪种模式、有没有偏移量，要按开发规则来。

### `pad()` 和 `unpad()`

AES 是分组加密，数据长度要满足分组要求。

```text
pad：加密前填充数据
unpad：解密后去掉填充
```

### `base64`

加密后的二进制结果不方便展示或传输，所以课程用 base64：

```text
加密二进制 -> base64 字符串 -> 传给接口
```

解密时反过来：

```text
base64 字符串 -> 二进制 -> AES 解密 -> 去填充 -> 明文
```

---

## 第 8 块：MD5

源码：`9月7号/examples_test/md5.py`

```python
import hashlib

md5 = hashlib.md5()
md5.update("123456".encode('utf-8'))
print(md5.hexdigest())
```

MD5 常用于生成哈希值。

它和 AES 不同：

```text
AES：可以加密，也可以解密
MD5：哈希，不可逆，不能直接解密
```

接口里常见用途：

```text
密码摘要
签名参数
文件校验
```

---

## 第 9 块：加密登录用例的 YAML 契约

后面课程用例里出现了这样的结构，源文件如 `9月8号/examples/api-cases-yaml/1_aeslogin.yaml`：

```yaml
- 数据加密-密码:
    操作类型: 加密aes
    data: '<原始密码>'
    VARNAME: aes_pwd

- 数据加密-用户名:
    操作类型: 加密aes
    data: '<原始用户名>'
    VARNAME: aes_username

- 发送加密接口:
    操作类型: 发送请求POST
    请求地址: http://127.0.0.1:8080/login_safe
    请求数据:
      username: "{{aes_username}}"
      password: "{{aes_pwd}}"
```

它表达的业务流程是：

```text
1. 加密用户名，保存到 aes_username
2. 加密密码，保存到 aes_pwd
3. 发送登录接口
4. 用 {{aes_username}}、{{aes_pwd}} 作为参数
```

这说明关键字不只能发请求，还能做“数据加工”。

---

## 接口测试遇到加密怎么办

课程笔记里总结了四种方式：

```text
1. 开发直接生成加密/解密数据
2. 开发提供 API 或 jar 包调用方式
3. 用外部加解密工具
4. 根据开发规则自己写加解密代码
```

常见类型：

```text
对称加密：AES、DES，加密和解密用同一类密钥
非对称加密：RSA，公钥加密、私钥解密
哈希：MD5、SHA 系列，不可逆
```

---

## 适合什么场景

- 头像上传、附件上传、图片上传。
- 导出文件、下载报表、下载日志。
- 登录密码加密。
- 接口签名。
- 文件完整性校验。

---

## 课后自测

1. `requests` 上传文件时，`files` 参数大概长什么样？
2. `open(file_path, 'rb')` 里的 `b` 为什么重要？
3. 什么情况下建议使用流式下载？
4. AES 和 MD5 的核心区别是什么？
5. 如果接口要求密码 AES 加密，你应该先问开发哪些信息？

---

# 第16节：9月8号，Excel 用例解析器

## 今天解决什么问题

YAML 对测试同学很友好，但有些团队更习惯 Excel：

```text
业务人员会用 Excel
可以在表格里批量维护
方便复制、筛选、评审
```

所以今天要把 Excel 用例读成和 YAML 一样的结构。

目标格式是：

```python
[
    {
        "基础配置": {...},
        "用例步骤": [...]
    }
]
```

这样后面的执行器就不用关心用例来自 YAML 还是 Excel。

---

## 第 1 块：Excel 文件规则

源码：`9月8号/HAT/parse/ExcelCaseParser.py`

```python
file_names = [
    (int(f.split("_")[0]), f)
    for f in os.listdir(suite_folder)
    if f.endswith(".xlsx") and f.split("_")[0].isdigit()
]
file_names.sort()
file_names = [f[-1] for f in file_names]
```

规则和 YAML 类似：

```text
1_shop.xlsx
2_API测试用例.xlsx
```

要求：

```text
以 .xlsx 结尾
文件名第一段是数字
按数字排序
```

---

## 第 2 块：用 pandas 读 Excel

源码核心：

```python
data = pd.read_excel(file_path)
data = data.where(data.notnull(), None)
data = data.to_dict(orient='records')
group_cases = group_cases_by_title(data)
```

### `pd.read_excel()`

pandas 会把 Excel 当前 sheet 读成 DataFrame。

你可以把 DataFrame 理解成“表格对象”，有表头、行、列。

### `data.where(data.notnull(), None)`

Excel 空单元格可能被 pandas 读成 `NaN`。

这里把 `NaN` 替换成 Python 的 `None`。

```text
NaN：pandas 里的缺失值
None：Python 里的空值
```

后面代码判断更统一。

### `to_dict(orient='records')`

把表格转成“每行一个字典”的列表。

比如 Excel 是：

| 用例标题 | 操作类型 | 数据内容 |
| --- | --- | --- |
| 登录 | 发送请求POST | 请求地址=xxx |

转成：

```python
[
    {
        "用例标题": "登录",
        "操作类型": "发送请求POST",
        "数据内容": "请求地址=xxx"
    }
]
```

后面就好处理了。

---

## 第 3 块：按用例标题分组

源码：`9月8号/HAT/parse/ExcelCaseParser.py`

```python
def group_cases_by_title(data):
    current_case = None
    result = []

    for row in data:
        title = row.get("用例标题")
        module_1 = row.get("模块")
        module_2 = row.get("功能")
        case_type = row.get("用例类型")
        step_desc = row.get("测试步骤")
        action_type = row.get("操作类型")
        data_content = row.get("数据内容")

        data_content_dict = {}
        if data_content is not None and data_content != "":
            pattern = r'(\w+)=(?:"([^"]*)"|(\S+))'
            matches = re.findall(pattern, data_content)

            for key, quoted_value, unquoted_value in matches:
                value = quoted_value if quoted_value else unquoted_value
                if value is not None:
                    data_content_dict[key] = safe_convert_value(value)
                else:
                    data_content_dict[key] = None

        if title is not None and title != "":
            if current_case is not None:
                result.append(current_case)

            current_case = {"基础配置": {}, "用例步骤": []}
            current_case["基础配置"].update({"用例类型": case_type})
            current_case["基础配置"].update({"用例标题": title})
            current_case["基础配置"].update({"一级模块": module_1})
            current_case["基础配置"].update({"二级模块": module_2})

        if current_case is not None:
            current_case["用例步骤"].append({
                step_desc: {
                    "操作类型": action_type,
                    **data_content_dict
                }
            })

    if current_case is not None:
        result.append(current_case)

    return result
```

### 为什么要分组

Excel 里一条用例通常占多行：

```text
第 1 行：有用例标题，是第一条用例的开始
第 2 行：没有标题，属于上一条用例的第二个步骤
第 3 行：没有标题，属于上一条用例的第三个步骤
第 4 行：又有标题，是第二条用例的开始
```

代码用 `current_case` 表示“当前正在组装的用例”。

流程：

```text
遇到新标题：
1. 如果 current_case 已存在，先收进 result
2. 创建新的 current_case

没遇到标题：
1. 继续 append 到 current_case 的用例步骤
```

最后：

```python
if current_case is not None:
    result.append(current_case)
```

因为最后一条用例不会再遇到“下一个标题”，要单独收尾。

---

## 第 4 块：解析数据内容

Excel 里的 `数据内容` 通常是一段文本，比如：

```text
请求地址=http://xxx 请求数据={"accounts":"xxx"}
```

代码用正则表达式拆：

```python
pattern = r'(\w+)=(?:"([^"]*)"|(\S+))'
matches = re.findall(pattern, data_content)
```

这个正则可以简单理解成：

```text
找一个 key=value 结构
value 可以是双引号包起来的内容
也可以是不带空格的普通内容
```

匹配后得到类似：

```python
[
    ('请求地址', 'http://xxx', ''),
    ('请求数据', '{"accounts":"xxx"}', '')
]
```

然后：

```python
value = quoted_value if quoted_value else unquoted_value
data_content_dict[key] = safe_convert_value(value)
```

优先取带引号的值，否则取不带引号的值。

---

## 第 5 块：safe_convert_value

源码：

```python
def safe_convert_value(value_str):
    if not isinstance(value_str, str):
        if isinstance(value_str, (int, float, bool)) or value_str is None:
            return value_str
        value_str = str(value_str)

    value_str = value_str.strip()

    try:
        return json.loads(value_str)
    except json.JSONDecodeError:
        pass

    try:
        return ast.literal_eval(value_str)
    except (SyntaxError, ValueError):
        return value_str
```

### 为什么要做类型转换

Excel 单元格里的内容读出来经常是字符串。

比如：

```text
"123" -> 想变成数字 123
"true" -> 想变成布尔 True
"{'a':1}" -> 想变成字典
```

### 转换顺序

```text
1. 先用 json.loads()
2. 失败再用 ast.literal_eval()
3. 还失败就保留原始字符串
```

`json.loads()` 适合标准 JSON：

```text
123
true
{"a":1}
```

`ast.literal_eval()` 适合 Python 字面量：

```text
{'a':1}
[1, 2, 3]
```

`ast.literal_eval()` 比 `eval()` 安全一些，因为它只解析字面量，不会执行任意表达式。

---

## 第 6 块：load_excel_files

源码核心：

```python
def load_excel_files(file_path):
    excel_caseInfos = []
    suite_folder = os.path.join(file_path)

    file_names = [...]
    file_names.sort()
    file_names = [f[-1] for f in file_names]

    for file_name in file_names:
        file_path = os.path.join(suite_folder, file_name)
        data = pd.read_excel(file_path)
        data = data.where(data.notnull(), None)
        data = data.to_dict(orient='records')
        group_cases = group_cases_by_title(data)

        for case in group_cases:
            excel_caseInfos.append(case)

    return excel_caseInfos
```

流程：

```text
1. 找到所有符合编号规则的 .xlsx 文件
2. 排序
3. 逐个读 Excel
4. 转成记录字典
5. 按标题分组
6. 放进总列表
```

---

## 第 7 块：课堂导出 yaml 文件

源码最后有：

```python
if __name__ == '__main__':
    c = load_excel_files('../../examples/api-cases-excel')
    with open("./excel数据.yaml", 'w', encoding="utf-8") as file:
        yaml.dump(c, file, allow_unicode=True, sort_keys=False)
```

这段是课堂调试用的。

它的意思是：

```text
读取 Excel
转换成统一结构
再写成一个 YAML 文件，方便同学看转换结果
```

`allow_unicode=True` 让中文正常显示。  
`sort_keys=False` 保持字典原来的键顺序。

---

## Excel 用例的好处和注意点

好处：

```text
业务同学熟悉
批量维护方便
适合表格化场景
```

注意点：

```text
数据内容是字符串，要做类型转换
空单元格要处理 NaN
一条用例多步骤时，要靠标题行分组
文件名编号仍然决定顺序
```

---

## 适合什么场景

- 非技术同事参与维护用例。
- 用例字段多、适合表格展示。
- 需要批量导入已有测试用例。
- 希望测试框架同时支持 YAML 和 Excel。

---

## 课后自测

1. Excel 文件命名规则是什么？
2. `data.where(data.notnull(), None)` 解决什么问题？
3. `to_dict(orient='records')` 把表格转成什么结构？
4. 为什么遇到新的“用例标题”要先把 `current_case` 放进 `result`？
5. `ast.literal_eval()` 和 `eval()` 有什么区别？

---

# 第17节：9月9号，Excel 全局配置 context.xlsx

## 今天解决什么问题

昨天已经能用 Excel 写用例了。

但全局配置还在 `context.yaml`，比如：

```text
URL
数据库配置
session_reuse
key_dir
```

既然用例可以用 Excel，配置也可以用 Excel。

所以今天要读：

```text
context.xlsx
```

并把里面的配置读进 `g_context`。

---

## 第 1 块：load_dbinfo

源码：`9月9号/HAT/parse/ExcelCaseParser.py`

```python
def load_dbinfo(name, all_sheets):
    db_config = all_sheets.fillna("").to_dict(orient="records")

    config_dict = {}
    for item in db_config:
        db_info = {}
        db_name = item.get("别名")
        host = item.get("服务器IP")
        port = item.get("端口号")
        user = item.get("用户名")
        password = item.get("密码")
        db = item.get("数据库名称")

        db_info.update({"host": host})
        db_info.update({"port": port})
        db_info.update({"user": user})
        db_info.update({"password": password})
        db_info.update({"db": db})

        config_dict[db_name] = db_info

    return config_dict
```

### 它处理哪个 sheet

后面你会看到：

```python
all_sheets = pd.read_excel(excel_file_path, sheet_name="数据库配置")
config_dict = load_dbinfo("数据库配置", all_sheets)
```

所以 `load_dbinfo()` 处理的是 `context.xlsx` 里名为“数据库配置”的 sheet。

### `fillna("")`

```python
db_config = all_sheets.fillna("").to_dict(orient="records")
```

把空单元格填成空字符串。

和昨天 `data.where(data.notnull(), None)` 不完全一样：

```text
这里用 fillna("")
昨天用 None
```

课程源码按当前函数需求处理，注意区分。

### 生成的结构

假设 Excel 里有一行：

| 别名 | 服务器IP | 端口号 | 用户名 | 密码 | 数据库名称 |
| --- | --- | --- | --- | --- | --- |
| 电商数据库 | xxx | 3306 | xxx | xxx | xxx |

函数会生成：

```python
{
    "电商数据库": {
        "host": "xxx",
        "port": 3306,
        "user": "xxx",
        "password": "xxx",
        "db": "xxx"
    }
}
```

这个结构和 YAML 里的 `_数据库` 保持一致，方便关键字继续使用。

---

## 第 2 块：load_configuration

源码：

```python
def load_configuration(name, all_sheets):
    config_dict = {row["配置名"]: row["配置值"] for _, row in all_sheets.iterrows()}

    for key, value in config_dict.items():
        config_dict[key] = safe_convert_value(value)

    return config_dict
```

### 它处理通用配置 sheet

后面会读：

```python
all_sheets = pd.read_excel(excel_file_path, sheet_name="通用配置")
config_dict = load_configuration("通用配置", all_sheets)
```

### `iterrows()`

pandas 的逐行遍历方法。

```python
for _, row in all_sheets.iterrows():
    ...
```

`row` 是一行的数据。

前面的 `_` 表示暂时不用的行索引。

### 字典推导式

```python
{row["配置名"]: row["配置值"] for _, row in all_sheets.iterrows()}
```

意思是：

```text
每一行都生成一个键值对
键来自“配置名”列
值来自“配置值”列
```

### 再做类型转换

```python
for key, value in config_dict.items():
    config_dict[key] = safe_convert_value(value)
```

Excel 里的 `True`、`123` 可能读成字符串，所以要用 `safe_convert_value()` 转回 Python 类型。

---

## 第 3 块：load_context_from_excel

源码：

```python
def load_context_from_excel(file_path):
    context_data = {}
    excel_file_path = os.path.join(file_path, 'context.xlsx')

    all_sheets = pd.read_excel(excel_file_path, sheet_name="数据库配置")
    config_dict = load_dbinfo("数据库配置", all_sheets)
    context_data.update({"_数据库": config_dict})

    all_sheets = pd.read_excel(excel_file_path, sheet_name="通用配置")
    config_dict = load_configuration("通用配置", all_sheets)
    context_data.update(config_dict)

    if context_data:
        g_context().set_by_dict(context_data)
```

### 流程

```text
1. 拼 context.xlsx 路径
2. 读“数据库配置” sheet
3. 转成 _数据库 配置
4. 读“通用配置” sheet
5. 转成普通全局变量
6. 全部放进 g_context
```

### `context_data.update({"_数据库": config_dict})`

这会生成：

```python
{
    "_数据库": {
        "电商数据库": {...},
        ...
    }
}
```

后面关键字继续这样取：

```python
g_context().get_dict("_数据库")["电商数据库"]
```

---

## 第 4 块：excel_case_parser

源码：

```python
def excel_case_parser(config_path):
    case_names = []
    cases_infos = []
    excel_caseInfos = load_excel_files(config_path)

    for caseinfo in excel_caseInfos:
        case_name = caseinfo.get("基础配置").get("用例标题", uuid.uuid4().__str__())
        case_names.append(case_name)
        cases_infos.append(caseinfo)

    return {
        "case_infos": cases_infos,
        "case_names": case_names
    }
```

### 它和 YAML 解析器的对应关系

YAML 有：

```python
yaml_case_parser()
```

Excel 有：

```python
excel_case_parser()
```

它们都返回相同结构：

```python
{
    "case_infos": [...],
    "case_names": [...]
}
```

这就是为第二天 `CaseParser` 做准备。

---

## 第 5 块：load_excel_files 里加载 context.xlsx

`9月9号/HAT/parse/ExcelCaseParser.py` 里，`load_excel_files()` 加了一行：

```python
load_context_from_excel(file_path)
```

所以读取 Excel 用例前，会先把 Excel 全局配置加载进 `g_context`。

流程变成：

```text
读取 context.xlsx
  -> 全局配置进入 g_context
读取编号 Excel 用例
  -> 转成统一用例结构
```

---

## 第 6 块：课堂调试导出

源码最后：

```python
if __name__ == '__main__':
    load_context_from_excel('../../examples/api-cases-excel')
    load_context = g_context().show_dict()
    with open("./context_excel数据.yaml", 'w', encoding="utf-8") as file:
        yaml.dump(load_context, file, allow_unicode=True, sort_keys=False)
```

这段代码不是执行测试，而是课堂调试：

```text
看看 Excel 配置读出来后，和 YAML 全局变量长不长一样
```

---

## 为什么要让 Excel 配置和 YAML 配置结构一致

因为后面的关键字不关心配置来源，只关心结构。

比如数据库关键字需要：

```python
g_context().get_dict("_数据库")["电商数据库"]
```

只要最终结构一样，配置来自：

```text
context.yaml
```

还是：

```text
context.xlsx
```

都可以。

这就是“统一数据结构”的好处。

---

## 适合什么场景

- 用例和配置都由业务团队用 Excel 维护。
- 测试环境地址、数据库、公共开关经常变化。
- 同一套执行器要兼容多种数据来源。

---

## 课后自测

1. `load_dbinfo()` 为什么要把 Excel 列名转成 `host/port/user/password/db`？
2. `iterrows()` 里的 `_` 表示什么？
3. 为什么通用配置要调用 `safe_convert_value()`？
4. `excel_case_parser()` 的返回结构为什么要和 `yaml_case_parser()` 一样？
5. 如果 `context.xlsx` 里少了“通用配置” sheet，程序会在哪里出问题？

---
# 第18节：9月10号，pytest 插件化、命令行传参和框架打包

## 今天解决什么问题

前面的框架已经很强，但还有一个别扭的地方：

```text
想换 YAML，要改 TestRunner.py
想换 Excel，也要改 TestRunner.py
想换用例目录，还是要改代码
```

比如原来类里面写死：

```python
alldata = yaml_case_parser(r'./examples/api-cases-yaml/')
data = alldata["case_infos"]
```

这就导致其他人不敢动核心代码。

今天的解决方案：

```text
用 pytest 插件收集用例
用命令行传参决定用例类型和目录
```

目标是让框架更像工具：

```text
--type=yaml --cases=./examples/api-cases-商城
```

---

## 第 1 块：CaseParser 统一入口

源码：`9月10号/HAT/parse/CaseParser.py`

```python
import os.path

from HAT.parse.ExcelCaseParser import excel_case_parser
from HAT.parse.YamlCaseParser import yaml_case_parser


def case_parser(case_type, case_dir):
    config_path = os.path.abspath(case_dir)

    if case_type == "yaml":
        return yaml_case_parser(config_path)

    if case_type == "excel":
        return excel_case_parser(config_path)

    return {"case_name": [], "case_info": []}
```

### 它解决什么问题

以前执行器可能这样：

```python
if 用例类型是 YAML:
    调 YAML 解析器
if 用例类型是 Excel:
    调 Excel 解析器
```

现在统一成一个函数：

```text
case_parser(类型, 目录)
```

以后调用方不用关心内部怎么分。

### `os.path.abspath(case_dir)`

把相对路径转成绝对路径。

比如：

```text
./examples/api-cases-商城
```

会转成当前机器上的完整路径。

这样避免运行目录不同导致找不到用例。

### 注意返回值的小细节

源码默认返回：

```python
{"case_name": [], "case_info": []}
```

而 YAML 和 Excel 解析器返回：

```python
{"case_names": [...], "case_infos": [...]}
```

键名不完全一样。课程资料里是这样写的，讲解时要按源码看清楚，不要自己改教学代码。  
实际使用时，建议你思考一下“统一返回结构”为什么很重要。

---

## 第 2 块：CasesPlugin 插件

源码：`9月10号/HAT/core/CasesPlugin.py`

```python
from HAT.core.globalContext import g_context
from HAT.parse.CaseParser import case_parser


class CasesPlugin:
    def pytest_addoption(self, parser):
        parser.addoption("--type", action="store", help="测试用例类型")
        parser.addoption("--cases", action="store", help="测试用例目录")
        parser.addoption("--keyDir", action="store", help="扩展关键字目录")

    def pytest_generate_tests(self, metafunc):
        case_type = metafunc.config.getoption("type")
        case_dir = metafunc.config.getoption("cases")
        key_dir = metafunc.config.getoption("keyDir")

        g_context().set_dict("key_dir", key_dir)

        data = case_parser(case_type, case_dir)

        if "caseinfo" in metafunc.fixturenames:
            metafunc.parametrize(
                "caseinfo",
                data["case_infos"],
                ids=data['case_names']
            )

    def pytest_collection_modifyitems(self, items):
        for item in items:
            item.name = item.name.encode("utf-8").decode("unicode_escape")
            item._nodeid = item.nodeid.encode("utf-8").decode("unicode_escape")
```

### 先理解“pytest 插件”

pytest 插件就是一个给 pytest 加功能的 Python 对象或模块。

它可以：

```text
添加命令行参数
改变用例收集方式
修改用例名称
集成日志、报告、重试等工具
```

课程里用一个类 `CasesPlugin` 承载了三个钩子函数。

### 钩子函数是什么

钩子函数可以理解成 pytest 留好的“插座”。

pytest 执行到某些节点时会主动调用：

```text
添加命令行参数时 -> pytest_addoption
收集用例、生成参数化时 -> pytest_generate_tests
用例收集完成后 -> pytest_collection_modifyitems
```

你只要实现对应方法，pytest 就会在合适时机调用它。

---

## 第 3 块：pytest_addoption

```python
def pytest_addoption(self, parser):
    parser.addoption("--type", action="store", help="测试用例类型")
    parser.addoption("--cases", action="store", help="测试用例目录")
    parser.addoption("--keyDir", action="store", help="扩展关键字目录")
```

### `parser.addoption()`

给 pytest 添加自定义命令行参数。

`action="store"` 表示把命令行后面的值保存起来。

比如：

```text
--type=yaml
--cases=./examples/api-cases-商城
--keyDir=./HAT/key_dir
```

### 为什么不用写死配置

有了这些参数：

```text
今天跑 YAML：--type=yaml
明天跑 Excel：--type=excel
换目录：改 --cases
换关键字目录：改 --keyDir
```

不用改 TestRunner.py。

---

## 第 4 块：pytest_generate_tests

```python
def pytest_generate_tests(self, metafunc):
    case_type = metafunc.config.getoption("type")
    case_dir = metafunc.config.getoption("cases")
    key_dir = metafunc.config.getoption("keyDir")
    ...
```

### `metafunc`

`metafunc` 是 pytest 给你的“用例生成信息”。

你可以通过它：

```text
读命令行参数
看测试函数有哪些参数
动态做参数化
```

### 读参数

```python
case_type = metafunc.config.getoption("type")
case_dir = metafunc.config.getoption("cases")
key_dir = metafunc.config.getoption("keyDir")
```

对应刚才添加的 `--type`、`--cases`、`--keyDir`。

### 保存 key_dir

```python
g_context().set_dict("key_dir", key_dir)
```

后面 `ex_invoke()` 动态扩展关键字时会用：

```python
g_context().get_dict("key_dir")
```

所以插件在这里把目录交给全局变量。

### 解析用例

```python
data = case_parser(case_type, case_dir)
```

根据 `--type` 决定调用：

```text
yaml_case_parser()
excel_case_parser()
```

### 动态参数化

```python
if "caseinfo" in metafunc.fixturenames:
    metafunc.parametrize(
        "caseinfo",
        data["case_infos"],
        ids=data['case_names']
    )
```

`TestRunner.test_case_exceute(self, caseinfo)` 有参数 `caseinfo`。

所以条件成立。

然后：

```text
data["case_infos"]：每条用例的数据
data["case_names"]：每条用例显示的标题
metafunc.parametrize()：动态生成多条用例
```

以前是这样：

```python
@pytest.mark.parametrize('caseinfo', data)
def test_case_exceute(self, caseinfo):
    ...
```

现在装饰器不用写死，pytest 插件会在收集阶段动态生成。

---

## 第 5 块：pytest_collection_modifyitems 解决中文乱码

```python
def pytest_collection_modifyitems(self, items):
    for item in items:
        item.name = item.name.encode("utf-8").decode("unicode_escape")
        item._nodeid = item.nodeid.encode("utf-8").decode("unicode_escape")
```

### `items`

收集到的所有测试用例列表。

每条用例有：

```text
item.name：用例名
item.nodeid：用例节点路径
```

### 为什么要编码再解码

因为中文用例标题在 pytest 收集时可能显示成类似 Unicode 转义的样子。

课程用：

```python
.encode("utf-8").decode("unicode_escape")
```

把显示形式转换回可读中文。

这是课堂资料里解决中文乱码的做法。

---

## 第 6 块：main.py 怎么使用插件

源码：`9月10号/main.py`

```python
import os
import sys
import time

import pytest
from allure_combine import combine_allure
from loguru import logger

from HAT.core.CasesPlugin import CasePlugin
```

注意源码里类是：

```text
HAT/core/CasesPlugin.py 里的 CasesPlugin
```

但 `main.py` 里导入的是：

```python
from HAT.core.CasesPlugin import CasePlugin
```

这是课程资料里需要特别注意的地方：导入名和类名目前不一致。教学文案不擅自修改课堂代码；实际运行或整理笔记时，要按老师课堂说明处理，不要只凭猜测改。

然后配置日志：

```python
if not os.path.exists('./HAT/logs'):
    os.mkdir('./HAT/logs')

time_str = time.strftime('%Y-%m-%d %H-%M-%S', time.localtime())
log_level = os.getenv("HAT_LOG_LEVEL", "DEBUG").upper()

logger.configure(
    handlers=[
        {"sink": sys.stdout, "level": "INFO"},
        {"sink": os.path.join("./HAT/logs", f"hat_{time_str}.log"), "level": log_level}
    ]
)
```

执行 pytest：

```python
pytest_ars = [
    '-v',
    '-s',
    '--capture=sys',
    '--clean-alluredir',
    '--alluredir=allure-results',
    './HAT/core/TestRunner.py',
    '--type=yaml',
    '--cases=./examples/api-cases-商城'
]

pytest.main(pytest_ars, plugins=[CasePlugin()])
```

这里最重要的一句：

```python
pytest.main(pytest_ars, plugins=[CasePlugin()])
```

它把自定义插件交给 pytest。

最后生成报告：

```python
os.system('allure generate -c -o allure-report')
combine_allure('./allure-report')
```

---

## 第 7 块：TestRunner 的变化

源码：`9月10号/HAT/core/TestRunner.py`

原来类上面有：

```python
alldata = yaml_case_parser(...)
data = alldata["case_infos"]

@pytest.mark.parametrize('caseinfo', data)
def test_case_exceute(self, caseinfo):
    ...
```

今天源码里这些被注释掉了：

```python
# alldata=yaml_case_parser(...)
# data=alldata["case_infos"]
# @pytest.mark.parametrize('caseinfo', data)
def test_case_exceute(self, caseinfo):
    ...
```

用例数据改由插件动态传入。

这样 TestRunner 只负责“执行一条用例”，不再负责“决定从哪里读用例”。

---

## 第 8 块：框架打包流程

课程笔记里的打包步骤：

```text
1. 创建虚拟环境，把 examples 和 HAT 放进去
2. pip freeze > requirement.txt 导出依赖
3. pip install -r requirement.txt -i 镜像 安装
4. main 文件内容更改
5. 增加 setup.py、README.md、allure_combine（complete.html）
6. python setup.py install
7. 报 no module 用 pip install -e .
```

### 为什么要打包

打包后可以给别人使用：

```text
同事不用复制你的开发目录
只需要安装依赖
运行命令就能测
```

### `pip freeze > requirement.txt`

导出当前环境依赖。

比如：

```text
pytest
requests
yaml
loguru
deepdiff
```

### `pip install -r requirement.txt`

按依赖文件安装。

### `pip install -e .`

以可编辑方式安装当前项目。

开发阶段很常用：

```text
改代码立即生效
不用每次重新安装
```

---

## 今天的核心思想

这一天表面在学 pytest 插件，其实在学框架设计：

```text
配置和代码分离
数据来源可插拔
核心执行器保持稳定
使用者通过命令行控制
```

这和很多企业级测试平台、测试工具的设计方向一致。

---

## 适合什么场景

- 框架要给多人使用。
- 项目里有 YAML 和 Excel 两类用例。
- 不同项目用例目录不同。
- 想把测试框架发布成内部工具。

---

## 课后自测

1. `pytest_addoption` 用来做什么？
2. `pytest_generate_tests` 里的 `metafunc` 有什么用？
3. `metafunc.parametrize()` 和装饰器 `@pytest.mark.parametrize` 有什么关系？
4. 插件为什么要用 `g_context().set_dict("key_dir", key_dir)`？
5. `pip install -e .` 和普通安装有什么区别？

---

# 第19节：9月11号，用 Flask 写 Mock 接口

## 今天解决什么问题

测试过程中，经常遇到这些情况：

```text
后端接口还没开发完
第三方接口不稳定
想构造异常响应，但真实接口不给造
想自己练习接口测试
```

这时候可以用 Mock 接口。

Mock 就是“假接口”：

```text
地址、参数、返回值都像真的
但数据是自己准备的
不依赖真实后端和数据库
```

今天用 Flask 从零写一个登录 Mock 接口。

---

## 第 1 块：最简单的 Flask 接口

源码：`9月11号/flask01_test.py`

```python
from flask import Flask, request

app = Flask(__name__)
```

这两行创建 Flask 应用。

```text
Flask：Web 框架
app：应用实例
```

### 定义 GET 路由

源码里有类似示例：

```python
@app.route('/list', methods=['get'])
def mock_test():
    return "首页列表查询成功"
```

意思是：

```text
访问 /list
请求方法是 GET
返回字符串
```

### 获取 GET 参数

源码里有：

```python
@app.route('/list', methods=['get'])
def mock_test():
    res = request.args.to_dict()
    print("获取所有数据", res)

    res = request.args.get("id")
    print("获取所有数据", res)

    print("请求头", request.headers)
    print("请求方式", request.method)
    print("请求地址", request.url)
    return "首页列表查询成功"
```

`request` 是 Flask 提供的请求对象。

常用属性：

```text
request.args：GET 参数
request.headers：请求头
request.method：请求方法
request.url：完整地址
```

### 获取 POST JSON 参数

源码最后保留的接口：

```python
@app.route('/login', methods=['post'])
def mock_test():
    res = request.get_json()
    print("获取json数据", res)
    return "你好"


if __name__ == '__main__':
    app.run(debug=True, host='0.0.0.0', port=8888)
```

这里要记住：

```text
GET 参数通常用 request.args
POST JSON 参数通常用 request.get_json()
```

### `app.run(...)` 参数

```python
app.run(debug=True, host='0.0.0.0', port=8888)
```

含义：

```text
debug=True：代码修改后自动重启，方便调试
host='0.0.0.0'：允许外部访问
port=8888：服务端口
```

---

## 第 2 块：带判断逻辑的登录 Mock

源码：`9月11号/flask02_test.py`

```python
from flask import Flask, request

app = Flask(__name__)

all_user = {"username": "<Mock用户名>", "password": "<Mock密码>"}


@app.route('/login', methods=['post'])
def mock_test():
    res = request.get_json()
    print("获取json数据", res)

    username = res["username"]
    password = res["password"]

    if username == all_user["username"] and password == all_user["password"]:
        return "登陆成功"
    elif username == "" or password == "":
        return "用户名或者密码不能为空"
    elif username != all_user["username"] or password != all_user["password"]:
        return "用户名或者密码错误"
    else:
        return "登陆失败"


if __name__ == '__main__':
    app.run(debug=True, host='0.0.0.0', port=8888)
```

课程源码里准备了具体的 Mock 用户名和密码，这里用占位符表示。

### 模拟真实系统的判断

真实项目里，用户名和密码通常在数据库里。

Mock 里没有数据库，所以先用字典：

```python
all_user = {...}
```

然后模拟后端逻辑：

```text
1. 接收 username、password
2. 和准备好的 all_user 对比
3. 全对：返回登陆成功
4. 有空值：返回不能为空
5. 不匹配：返回用户名或密码错误
```

### 这段代码的测试价值

它让你能自己构造这些场景：

```text
正确用户名 + 正确密码
错误用户名 + 正确密码
正确用户名 + 错误密码
用户名为空
密码为空
```

如果真实接口不方便造这些数据，Mock 就很有用。

---

## 第 3 块：返回标准 JSON

源码：`9月11号/flask03_test.py`

前面返回的是字符串：

```python
return "登陆成功"
```

但真实接口经常返回 JSON：

```json
{
  "msg": "登录成功",
  "code": 0,
  "data": {...}
}
```

所以源码里定义了几个响应字典：

```python
MSG_LOGIN_SUCCESS = {...}
USERNAME_OR_PASSWORD_ERROR = {...}
NOT_NULL = {...}
fail = {...}
```

其中 `MSG_LOGIN_SUCCESS` 里有用户信息、token、时间等字段，完整内容以课程源码为准。  
token 属于敏感值，写笔记或分享时不要照抄。

### 启动时关闭中文转义

```python
app.config['JSON_AS_ASCII'] = False
```

Flask 返回 JSON 时，中文有时会变成 Unicode 转义形式。

设置：

```python
JSON_AS_ASCII = False
```

可以让响应里的中文更直观。

### 用 jsonify 返回

```python
if username == all_user["username"] and password == all_user["password"]:
    return jsonify(MSG_LOGIN_SUCCESS)
elif username == "" or password == "":
    return jsonify(NOT_NULL)
elif username != all_user["username"] or password != all_user["password"]:
    return jsonify(USERNAME_OR_PASSWORD_ERROR)
else:
    return jsonify(fail)
```

`jsonify()` 的作用：

```text
把 Python 字典转成标准 JSON 响应
设置响应格式为 application/json
```

### 为什么 Mock 也要返回真实格式

因为客户端和测试脚本通常解析的是 JSON。

如果 Mock 返回字符串，就无法练习：

```text
jsonpath 提取 token
断言 code
断言 msg
深度对比 data
```

所以第三版接口更接近真实项目。

---

## Mock 接口和真实接口的关系

Mock 不是替代真实接口，而是补充。

```text
开发早期：用 Mock 让客户端和测试先动起来
联调阶段：切换真实接口
回归阶段：真实接口为主，Mock 用于特殊场景
```

常见 Mock 场景：

```text
接口未完成
第三方支付不稳定
超长文本
特殊字符
服务器异常
慢响应
特定错误码
```

---

## 用 Flask 写 Mock 的基本套路

以后你自己写 Mock，可以按这个顺序：

```text
1. import Flask
2. 创建 app
3. 定义路由和方法
4. 接收参数
5. 写判断或返回数据
6. 返回 JSON 或字符串
7. 启动服务
```

---

## 适合什么场景

- 接口还没开发完成。
- 想练习接口自动化。
- 想模拟异常分支。
- 想临时替代第三方接口。
- 想给前端提供联调假数据。

---

## 课后自测

1. `@app.route('/login', methods=['post'])` 定义了什么？
2. `request.args` 和 `request.get_json()` 分别适合什么时候用？
3. `jsonify()` 和直接 return 字符串有什么区别？
4. `JSON_AS_ASCII=False` 解决什么问题？
5. 为什么 Mock 接口要尽量模拟真实返回结构？

---

# 附：Python 基础卡片

这份课程里出现了不少 Python 写法。遇到看不懂的，先来这里查。

## 1. 类、对象、`__init__`、`self`

```python
class Keywords:
    def __init__(self, request):
        self.request = request
```

```text
class：定义类
__init__：构造方法，创建对象时自动执行
self：当前对象自己
self.request：把 request 保存到对象身上
```

比如：

```python
keywords = Keywords(requests)
```

创建对象时，`requests` 传给 `request`，后面对象里就能用 `self.request`。

---

## 2. 类变量和实例变量

```python
class g_context:
    _dic = {}
```

`_dic` 写在类里面、方法外面，是类变量。

```text
类变量：所有对象共享
实例变量：每个对象自己一份，通常在 __init__ 里用 self.xxx 定义
```

---

## 3. `**kwargs`

```python
def 发送请求POST(self, **kwargs):
    ...
```

`**kwargs` 接收任意数量的关键字参数，收成一个字典。

调用：

```python
发送请求POST(请求地址='xxx', 请求类型='json')
```

方法里：

```python
kwargs = {'请求地址': 'xxx', '请求类型': 'json'}
```

反过来：

```python
key_func(**step_value)
```

是把字典拆成关键字参数传进去。

---

## 4. `try / except / raise`

```python
try:
    ...
except AttributeError:
    ...
except Exception as e:
    raise e
```

```text
try：尝试执行
except：捕获某种异常
raise：主动抛出异常
```

pytest 里，测试方法抛异常通常表示用例失败。

---

## 5. f-string

```python
f"当前步骤:{step_name}"
```

f-string 用来格式化字符串，花括号里可以放变量或表达式。

---

## 6. 列表推导式

```python
[(int(f.split('_')[0]), f) for f in os.listdir(path) if f.endswith('.yaml')]
```

它相当于：

```python
result = []
for f in os.listdir(path):
    if f.endswith('.yaml'):
        result.append((int(f.split('_')[0]), f))
```

---

## 7. 字典推导式

```python
{row["配置名"]: row["配置值"] for _, row in sheet.iterrows()}
```

每一行生成一个键值对。

---

## 8. `next(iter(dict.items()))`

```python
step_name, step_value = next(iter(step.items()))
```

```text
dict.items()：拿到键值对
iter()：创建迭代器
next()：取下一个，也就是第一对
```

课程里每个步骤字典只有一个键值对，所以这样取。

---

## 9. `__getattribute__`

```python
keywords.__getattribute__('发送请求POST')
```

按字符串找对象的方法。

等价于：

```python
getattr(keywords, '发送请求POST')
```

注意拿到的是方法本身，还要加括号才执行。

---

## 10. `getattr`

```python
class_ = getattr(module, key)
```

从对象或模块里按字符串取属性。

常配合 `__import__` 使用。

---

## 11. `__import__`

```python
module = __import__('发送请求POST')
```

按字符串导入模块。

课程用它加载 `key_dir` 里的同名扩展关键字文件。

---

## 12. lambda

```python
lambda a, b: a == b
```

匿名函数。

等价于：

```python
def f(a, b):
    return a == b
```

课程把多个 lambda 放进 `comparators` 字典，用比较符查找函数。

---

## 13. eval

```python
eval("{'name': 'youer'}")
```

把字符串按 Python 字面量解析成对象。

```text
字符串 -> 字典/列表/数字等
```

它能执行表达式，有安全风险，不要处理不可信输入。

---

## 14. exec

```python
exec("context.update({'name': 'youer'})", {'context': context})
```

把字符串当代码执行。

第二个参数提供这段字符串代码里能用的变量。

它比 eval 更危险，同样只适合可信内容。

---

## 15. 深拷贝和浅拷贝

```python
copy.deepcopy(obj)
```

深拷贝会复制嵌套对象，新旧对象基本独立。

浅拷贝只复制最外层，嵌套内容可能还共享。

课程生成多条 DDT 用例时必须深拷贝，避免互相影响。

---

## 16. Path / pathlib

```python
from pathlib import Path

path = Path(file_path)
```

面向对象的路径操作。

```python
PROJECT_DIR / path
```

里的 `/` 是路径拼接。

---

## 17. os.path

```python
os.path.join(folder, 'context.yaml')
os.path.abspath(path)
os.path.exists(path)
os.makedirs(path, exist_ok=True)
```

```text
join：拼接路径
abspath：转绝对路径
exists：判断是否存在
makedirs：创建目录
```

课程早期用 `os.path`，后来也用了 `pathlib`，两者都常见。

---

## 18. 装饰器

```python
@allure.step('发送请求POST')
def 发送请求POST(self, **kwargs):
    ...
```

装饰器给函数加额外能力，不改变调用方式。

```text
@allure.step：给方法加 Allure 步骤
@contextmanager：让生成器函数支持 with
```

---

## 19. contextmanager 和 with

```python
@contextmanager
def allure_step_with_log(step_name):
    with allure.step(step_name):
        with StepLogCollector() as collector:
            yield collector
```

```text
with：进入和离开时自动做准备工作、清理工作
yield：暂停，让 with 里的代码执行
```

---

## 20. `global`

```python
_global_request_obj = None

def init_keywords(self):
    global _global_request_obj
    _global_request_obj = requests.session()
```

函数里要修改模块级全局变量时，需要 `global` 声明。

---

## 21. isinstance

```python
isinstance(data, dict)
isinstance(target, (dict, list))
```

判断对象是否是某种类型。

第二个参数也可以传元组，表示“是其中任意一种”。

---

## 22. with open

```python
with open(path, 'r', encoding='utf-8') as f:
    data = f.read()
```

```text
r：读文本
w：写文本，会覆盖
a：追加
rb：读二进制
wb：写二进制
with：代码块结束自动关闭文件
```

---

## 23. JSON 和 Python 对象

```python
json.dumps(obj)   # Python 对象 -> JSON 字符串
json.loads(s)     # JSON 字符串 -> Python 对象
```

常用对应：

```text
Python dict <-> JSON object
Python list <-> JSON array
Python str <-> JSON string
Python int/float <-> JSON number
Python True/False <-> JSON true/false
Python None <-> JSON null
```

---

## 24. yaml.safe_load

```python
data = yaml.safe_load(file)
```

把 YAML 解析成 Python 对象。

```yaml
URL: http://xxx
```

变成：

```python
{'URL': 'http://xxx'}
```

---

## 25. jsonpath 的结果

```python
result = jsonpath.jsonpath(response, '$..token')
ex_data = result[0]
```

`jsonpath` 匹配到的通常是列表。

所以要用下标取具体值。

找不到时它不会像你期待的那样返回空列表，课程里用：

```python
if not result:
    raise Exception(...)
```

及时发现。

---

## 26. requests 的 data 和 json

```python
requests.post(url, data=xxx)
requests.post(url, json=xxx)
```

```text
data：表单格式，常用 application/x-www-form-urlencoded
json：JSON 格式，常用 application/json
```

课程关键字用 `请求类型` 控制最终用哪个。

---

## 27. requests.Session

```python
session = requests.session()
session.request('post', **data)
```

Session 对象会维护会话状态，比如 Cookie。

课程用 `session_reuse` 配置决定是否复用同一个 Session。

---

## 28. pytest.mark.parametrize

```python
@pytest.mark.parametrize('caseinfo', data)
def test_case_exceute(self, caseinfo):
    ...
```

把列表里的每一条数据生成一次用例。

`data` 有 3 条，测试函数就执行 3 次。

---

## 29. allure 常用写法

```python
allure.dynamic.title('用例标题')
allure.dynamic.feature('一级模块')
allure.dynamic.story('二级模块')

with allure.step('步骤名称'):
    ...

allure.attach(
    '附件内容',
    name='步骤日志',
    attachment_type=allure.attachment_type.TEXT
)
```

```text
dynamic：给当前用例动态设置报告信息
with allure.step：定义报告里的步骤
allure.attach：给报告加附件
```

---

## 30. loguru

```python
logger.debug('调试')
logger.info('正常')
logger.warning('警告')
logger.error('错误')

logger.add('log.log', rotation='100MB', retention='10days')
logger.remove(sink_id)
```

```text
add：新增日志输出位置
remove：移除输出位置
rotation：轮转规则
retention：保留规则
```

---

## 31. pandas 读 Excel

```python
import pandas as pd

data = pd.read_excel(path)
data = data.where(data.notnull(), None)
rows = data.to_dict(orient='records')
```

```text
read_excel：读表格
where/notnull：处理缺失值
to_dict(orient='records')：每行转一个字典
```

---

## 32. 正则表达式片段

```python
pattern = r'(\w+)=(?:"([^"]*)"|(\S+))'
matches = re.findall(pattern, text)
```

```text
\w+：一个或多个字母、数字、下划线
[^"]*：除了双引号以外的内容
\S+：非空白内容
|：或者
re.findall：找出所有匹配
```

课程用它解析 Excel 的 `key=value` 文本。

---

## 33. pymysql 七步法

```python
connect = pymysql.connect(...)
cursor = connect.cursor()
sql = '...'
cursor.execute(sql)
rs = cursor.fetchall()
cursor.close()
connect.close()
```

记住顺序：

```text
连接 -> 游标 -> SQL -> 执行 -> 取结果 -> 关游标 -> 关连接
```

---

## 34. Flask 三件套

```python
from flask import Flask, request, jsonify

app = Flask(__name__)

@app.route('/login', methods=['post'])
def login():
    data = request.get_json()
    return jsonify({'msg': 'ok'})

app.run(debug=True)
```

```text
Flask：创建应用
request：拿请求参数
jsonify：返回 JSON 响应
route：定义路由和请求方法
```

---

## 学习建议

1. 每天不要只复制代码，先画出数据流向。
2. 遇到不懂的 Python 语法，回这张基础卡片查。
3. 课后自测只出题不给答案，是为了逼你自己动手验证。
4. 看课程源码时，重点看“数据从哪里来，变成什么，到哪里去”。
5. 学完整个课程后，你应该能自己说清一条接口用例从 YAML 到报告的完整生命周期。


