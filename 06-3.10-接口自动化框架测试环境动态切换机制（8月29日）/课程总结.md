# 第06课｜3.10 接口自动化框架测试环境动态切换机制

- 学习日期：8月29日
- 课程阶段：Pytest/YAML 框架搭建

## 本节核心内容

- 用 `g_context` 做全局变量；类变量 `_dic` 实现共享。
- `set_dict`、`set_by_dict`、`get_dict`、`show_dict`。
- 从 `context.yaml` 加载环境地址、账号等。
- YAML 中用 `{{变量名}}`；Jinja2 `Template` + `refresh` 渲染；执行前 `deepcopy` + `eval` 还原数据。

## 主要代码/文件

- `HAT/core/globalContext.py`
- `HAT/parse/YamlCaseParser.py`
- `HAT/utils/VarRender.py`
- `HAT/core/TestRunner.py`
- `context.yaml`

## 备注

本文件由 `19节课学习总结.md` 拆分，方便在当天课程文件夹中复习。
