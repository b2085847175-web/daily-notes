# 查漏补缺

## 文件读取

- 普通文本文件用 `open()` 读取，建议加 `encoding='utf-8'`，避免中文乱码
- 读取 YAML 文件用 `yaml.load(f, Loader=yaml.FullLoader)`
- 读取后返回的类型由 YAML 内容决定：
  - 字典格式返回 `dict`
  - 列表格式返回 `list`

### 普通文件读取

```python
with open('data.txt', 'r', encoding='utf-8') as f:
    content = f.read()

print(content)
```

- `r`：读文件
- `w`：写文件，会清空原内容
- `a`：追加内容
- `with open(...)`：自动关闭文件

### 读取 YAML 文件

```python
import yaml

with open('login.yaml', 'r', encoding='utf-8') as f:
    data = yaml.load(f, Loader=yaml.FullLoader)

print(data)
```

### 路径注意

- Windows 路径里的 `\` 可能引起转义问题，比如 `\t`、`\n`
- 推荐写成原始字符串：

```python
path = r'F:\课堂派\接口自动化\examples\login.yaml'
```

- 也可以直接使用正斜杠：

```python
path = 'F:/课堂派/接口自动化/examples/login.yaml'
```

- 相对路径根据运行程序时所在的目录来解析：
  - `./`：当前目录
  - `../`：上一级目录
