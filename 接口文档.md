# 商城接口文档（shop-xo.hctestedu.com）

> 来源：F:\learn-接口\资料\day08\requests接口流程实战.py

## 1. 登录接口

- 请求地址：`http://shop-xo.hctestedu.com/index.php?s=/api/user/login&application=app&application_client_type=weixin`
- 请求方式：POST
- 请求数据：

```json
{
    "accounts": "youtian",
    "pwd": "123456",
    "type": "username"
}
```

- 返回结果：`data.token`（token）、`msg`（登录成功）

## 2. 加入购物车接口

- 请求地址：`http://shop-xo.hctestedu.com/index.php?s=/api/cart/save&application=app&application_client_type=weixin&token={token}`
- 请求方式：POST
- 请求数据：

```json
{
    "goods_id": "2",
    "stock": "3"
}
```

- 返回结果：`msg`（加入成功）

## 3. 查看购物车接口

- 请求地址：`http://shop-xo.hctestedu.com/index.php?s=/api/cart/index&application=app&application_client_type=weixin&token={token}`
- 请求方式：GET
- 返回结果：`data.data[0].goods_id`（提交订单接口需要）

## 4. 查看地址列表接口

- 请求地址：`http://shop-xo.hctestedu.com/index.php?s=/api/useraddress/index&application=app&application_client_type=weixin&token={token}`
- 请求方式：POST
- 返回结果：`data.data[0].id`（提交订单接口需要，新账号可能没有地址）

## 5. 提交订单接口

- 请求地址：`http://shop-xo.hctestedu.com/index.php?s=/api/buy/add/index&application=app&application_client_type=weixin&token={token}`
- 请求方式：POST
- 请求数据：

```json
{
    "buy_type": "goods",
    "goods_id": "{goodsid}",
    "stock": "2",
    "spec": [],
    "address_id": "{addressid}",
    "payment_id": "3",
    "site_model": 0,
    "user_note": ""
}
```

- 返回结果：`msg`（提交成功）
