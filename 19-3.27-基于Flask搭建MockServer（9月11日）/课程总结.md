# 第19课｜3.27 基于Flask搭建MockServer

- 学习日期：9月11日
- 课程阶段：Excel、插件与 Mock

## 本节核心内容

- Mock 的意义和场景。
- 开发 Mock 前要知道接口地址、请求方式、请求参数、正常/异常返回。
- Flask 与 Django 对比；Flask 核心结构 `Flask(__name__)`、`@app.route`、`app.run`。
- GET 用 `request.args`，POST 用 `request.data`，JSON 用 `request.json/get_json`。
- `debug`、`host`、`port`；从返回字符串逐步升级到 `jsonify` 标准 JSON 响应。

## 主要代码/文件

- `flask01_test.py`
- `flask02_test.py`
- `flask03_test.py`

## 备注

本文件由 `19节课学习总结.md` 拆分，方便在当天课程文件夹中复习。
