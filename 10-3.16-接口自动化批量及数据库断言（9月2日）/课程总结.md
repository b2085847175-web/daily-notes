# 第10课｜3.16 接口自动化批量及数据库断言

- 学习日期：9月2日
- 课程阶段：断言、数据库与数据驱动

## 本节核心内容

- 普通字段断言的不足。
- 用 `deepdiff` 做整个结构深度比对；`values_changed`、缺失字段等差异类型。
- `exclude_paths` 过滤动态字段；`ignore_string_case` 忽略大小写；`ignore_order` 忽略顺序。
- `assert not diff`；封装“批量断言”关键字。

## 主要代码/文件

- `examples_test/deepdiff1_test.py`
- `deepdiff2_test.py`
- `api_keywords.py` 中的批量断言

## 备注

真正的 MySQL 数据库操作和数据库断言在下一节 11 展开。
