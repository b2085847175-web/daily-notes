# 第14课｜3.20 Session复用在接口自动化框架应用与实现

- 学习日期：9月6日
- 课程阶段：日志、Session 与常用案例

## 本节核心内容

- requests 每次请求独立的问题。
- cookie/session 鉴权和 token 鉴权的区别。
- `context.yaml` 增加 `session_reuse` 开关；`True` 时只创建一个 `requests.session()` 并复用，`False` 时每次新建。
- 用 `ApiCaseContext.init_keywords()` 统一初始化。
- 继续优化 `key_dir` 关键字外部调用。

## 主要代码/文件

- `HAT/context/ApiCaseContext.py`
- `api_keywords.ex_invoke`
- `HAT/core/TestRunner.py`

## 备注

本文件由 `19节课学习总结.md` 拆分，方便在当天课程文件夹中复习。
